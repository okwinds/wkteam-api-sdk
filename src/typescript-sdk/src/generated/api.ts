/* eslint-disable */
// @generated by tools/generate_sdk.mjs - DO NOT EDIT

import type { EndpointCallOptions } from "../sdk/config.js";
import { WkteamValidationError } from "../sdk/errors.js";
import type { WkteamCoreClient } from "../sdk/core-client.js";
import { validators } from "./validators.js";

export type Params_biao_qian_addContactLabel = {
  /** 登录实例标识 */
  "wId": string;
  /** 标签名称 */
  "labelName": string;
  [k: string]: unknown;
};

export type Params_biao_qian_delContactLabel = {
  /** 登录实例标识 */
  "wId": string;
  /** 标签标识，多个标签已 "，" 号分割 */
  "labelIdList": string;
  [k: string]: unknown;
};

export type Params_biao_qian_getContactLabelList = {
  /** 登录实例标识 */
  "wId": string;
  [k: string]: unknown;
};

export type Params_biao_qian_modifyContactLabel = {
  /** 登录实例标识 */
  "wId": string;
  /** 好友微信id */
  "wcId": string;
  /** 标签标识，多个标签已 "，" 号分割 */
  "labelIdList": string;
  [k: string]: unknown;
};

export type Params_deng_lu_deng_lu_wei_kong_ping_tai_di_yi_bu = {
  /** **开发者**账号 */
  "account": string;
  /** **开发者**密码 */
  "password": string;
  [k: string]: unknown;
};

export type Params_deng_lu_er_ci_deng_lu = {
  /** 微信id（登录接口返回的wcId） */
  "wcId": string;
  /** 网络类型2，若上次登录使用ttuid，本参数则必传，反之则不传 */
  "ttuid"?: string;
  "aid"?: string;
  [k: string]: unknown;
};

export type Params_deng_lu_huo_qu_wei_xin_er_wei_ma2 = {
  /** 微信原始id （首次登录平台的号传""，掉线重登必须传值，否则会频繁掉线！！！） [第3步](zhi-xing-wei-xin-deng-lu.html)会返回此字段，记得入库保存 */
  "**wcId**": string;
  /** 设备类型：ipad【推荐】、mac */
  "deviceType": string;
  "proxy": number;
  [k: string]: unknown;
};

export type Params_deng_lu_initFriendList = {
  /** 登录实例标识 */
  "wId": string;
  [k: string]: unknown;
};

export type Params_deng_lu_queryFriendList = {
  /** 登录实例标识 */
  "wId": string;
  [k: string]: unknown;
};

export type Params_deng_lu_zhang_hao_mi_ma_deng_lu = {
  /** **登录实例标识** */
  "wId"?: string;
  /** 微信原始id （首次登录平台的号传""，掉线重登必须传值，否则会频繁掉线！！！） */
  "**wcId**"?: string;
  "proxy": number;
  [k: string]: unknown;
};

export type Params_deng_lu_zhi_xing_wei_xin_deng_lu = {
  /** **登录实例标识** */
  "wId": string;
  /** 是否自动验证【说明:设备类型为Mac时使用，其它情况传false】 */
  "autoCheck": boolean;
  /** 验证码 */
  "verifyCode"?: string;
  [k: string]: unknown;
};

export type Params_hao_you_cao_zuo_acceptUser = {
  /** 登录实例标识 */
  "wId": string;
  /** v1（从消息回调中取） */
  "v1": string;
  /** v2（从消息回调中取） */
  "v2": string;
  /** 取回调中的scene来源 */
  "type": number;
  [k: string]: unknown;
};

export type Params_hao_you_cao_zuo_addFriend = {
  /** 登录实例标识 */
  "wId": string;
  /** v1 从[搜索好友接口](serchUser.html)获取 */
  "v1": string;
  /** v2 从[搜索好友接口](serchUser.html)获取 */
  "v2": string;
  "type": number;
  [k: string]: unknown;
};

export type Params_hao_you_cao_zuo_checkZombie = {
  /** 登录实例标识 */
  "wId": string;
  /** 好友微信id，多个已","分隔,每次最多支持个20 */
  "wcId": string;
  [k: string]: unknown;
};

export type Params_hao_you_cao_zuo_getImAddressList = {
  /** 登录实例标识 */
  "wId": string;
  [k: string]: unknown;
};

export type Params_hao_you_cao_zuo_getOpenImContact = {
  /** 登录实例标识 */
  "wId": string;
  /** 企微好友微信id */
  "wcId": string;
  [k: string]: unknown;
};

export type Params_hao_you_cao_zuo_huo_qu_zi_ji_de_er_wei_ma = {
  /** 登录实例标识 */
  "wId": string;
  [k: string]: unknown;
};

export type Params_hao_you_cao_zuo_queryUserInfo = {
  /** 登录实例标识 */
  "wId": string;
  /** 好友微信id/群id,多个好友/群 以","分隔每次最多支持20个微信/群号,本接口每次调用请随机间隔300ms-800ms之间 */
  "wcId": string;
  [k: string]: unknown;
};

export type Params_hao_you_cao_zuo_serchUser = {
  /** 登录实例标识 */
  "wId": string;
  /** 微信号/手机号 (不支持微信id搜索) */
  "wcId": string;
  [k: string]: unknown;
};

export type Params_hao_you_cao_zuo_setDisturb = {
  /** 登录实例标识 */
  "wId": string;
  /** 好友id/群id */
  "chatRoomId": string;
  /** 0：开启 1：关闭 */
  "type": number;
  [k: string]: unknown;
};

export type Params_hao_you_cao_zuo_setFriendPermission = {
  /** 微信实列ID */
  "wId": string;
  /** 好友微信id */
  "wcId": string;
  /** 1:正常 2:仅聊天 */
  "type": number;
  [k: string]: unknown;
};

export type Params_hao_you_cao_zuo_setTop = {
  /** 登录实例标识 */
  "wId": string;
  /** 好友id/群id */
  "wcId": string;
  /** 0：取消 1：置顶 */
  "operType": number;
  [k: string]: unknown;
};

export type Params_hao_you_cao_zuo_shan_chu_hao_you = {
  /** 微信实列ID */
  "wId": string;
  /** 需删除的微信id */
  "wcId": string;
  [k: string]: unknown;
};

export type Params_hao_you_cao_zuo_she_zhi_ge_ren_tou_tou_xiang = {
  /** 登录实例标识 */
  "wId": string;
  /** 图片url链接 */
  "path": string;
  [k: string]: unknown;
};

export type Params_hao_you_cao_zuo_userPrivacySettings = {
  /** 登录实例标识 */
  "wId": string;
  "privacyType": number;
  [k: string]: unknown;
};

export type Params_hao_you_cao_zuo_xiu_gai_hao_you_bei_zhu = {
  /** 登录实例标识 */
  "wId": string;
  /** 好友微信id */
  "wcId": string;
  /** 好友备注 */
  "remark": string;
  [k: string]: unknown;
};

export type Params_peng_you_quan_asynSnsSendVideo = {
  /** 登录实例标识 */
  "wId": string;
  /** 文本内容 */
  "content": string;
  /** 视频链接URL 最大支持20M且30秒内 */
  "videoPath": string;
  /** 视频封面URL 最大支持2M内 */
  "thumbPath": string;
  /** 对谁可见（传微信号,多个用,分隔） */
  "groupUser"?: string;
  /** 对谁不可见（传微信号,多个用.分隔） */
  "blackList"?: string;
  [k: string]: unknown;
};

export type Params_peng_you_quan_deleteSns = {
  /** 登录实例标识 */
  "wId": string;
  /** 朋友圈id */
  "id": string;
  [k: string]: unknown;
};

export type Params_peng_you_quan_downloadSnsVideo = {
  /** 登录实例标识 */
  "wId": string;
  /** 通过获取某条朋友圈详细内容接口\[/getSnsObject\]返回的xml */
  "content": string;
  [k: string]: unknown;
};

export type Params_peng_you_quan_forwardSns = {
  /** 登录实例标识 */
  "wId": string;
  /** 收到的xml */
  "content": string;
  /** 对谁不可见（传微信id,多个用,分隔） */
  "blackList"?: string;
  /** 对谁可见 （传微信id,多个用,分隔） */
  "withUserList"?: string;
  [k: string]: unknown;
};

export type Params_peng_you_quan_getAsynSnsSendVideoRes = {
  /** 异步发送视频朋友圈返回的asynId */
  "asynId": string;
  [k: string]: unknown;
};

export type Params_peng_you_quan_getCircle = {
  /** 登录实例标识 */
  "wId": string;
  "firstPageMd5": string;
  [k: string]: unknown;
};

export type Params_peng_you_quan_getFriendCircle = {
  /** 登录实例标识 */
  "wId": string;
  /** 微信id */
  "wcId": string;
  /** 首页传:""，第2页及以后传返回的firstPageMd5 （PS：firstPageMd5为null情况下，则用上次不为null的值） */
  "firstPageMd5": string;
  /** 首页传：0（PS：第2页及以后用返回数据最后一个条目的id） */
  "maxId": number;
  [k: string]: unknown;
};

export type Params_peng_you_quan_getSnsObject = {
  /** 登录实例标识 */
  "wId": string;
  /** 好友微信id */
  "wcId": string;
  /** 朋友圈标识 */
  "id": string;
  [k: string]: unknown;
};

export type Params_peng_you_quan_snsCancelPraise = {
  /** 登录实例标识 */
  "wId": string;
  /** 朋友圈id */
  "id": string;
  [k: string]: unknown;
};

export type Params_peng_you_quan_snsComment = {
  /** 登录实例标识 */
  "wId": string;
  /** 朋友圈标识 */
  "id": string;
  /** 评论标识（回复评论） */
  "replyCommentId": number;
  /** 内容 */
  "content": string;
  [k: string]: unknown;
};

export type Params_peng_you_quan_snsCommentDel = {
  /** 登录实例标识 */
  "wId": string;
  /** 朋友圈id */
  "id": string;
  /** 评论id */
  "commentId": number;
  [k: string]: unknown;
};

export type Params_peng_you_quan_snsPraise = {
  /** 登录实例标识 */
  "wId": string;
  /** 朋友圈Id */
  "id": string;
  [k: string]: unknown;
};

export type Params_peng_you_quan_snsPrivacySettings = {
  /** 登录实例标识 */
  "wId": string;
  "type": number;
  [k: string]: unknown;
};

export type Params_peng_you_quan_snsSend = {
  /** 登录实例标识 */
  "wId": string;
  /** 文本内容 */
  "content": string;
  /** 对谁可见（传微信id,多个用,分隔） */
  "groupUser"?: string;
  /** 对谁不可见（传微信id,多个用,分隔） */
  "blackList"?: string;
  /** 对谁可见（传标签id,多个用,分隔） */
  "groupUserLabelIds"?: string;
  /** 对谁不可见（传标签id,多个用,分隔） */
  "blackListLabelIds"?: string;
  [k: string]: unknown;
};

export type Params_peng_you_quan_snsSendImage = {
  /** 登录实例标识 */
  "wId": string;
  /** 文本内容 */
  "content": string;
  /** 图片url(多个用;分隔 单张图片最大3M以内,必须是png格式) */
  "paths": string;
  /** 对谁可见（传微信id,多个用,分隔） */
  "groupUser"?: string;
  /** 对谁不可见（传微信id,多个用,分隔） */
  "blackList"?: string;
  /** 对谁可见（传标签id,多个用,分隔） */
  "groupUserLabelIds"?: string;
  /** 对谁不可见（传标签id,多个用,分隔） */
  "blackListLabelIds"?: string;
  [k: string]: unknown;
};

export type Params_peng_you_quan_snsSendUrl = {
  /** 登录实例标识 */
  "wId": string;
  /** 文本内容 */
  "content": string;
  /** 标题 */
  "title": string;
  /** 描述 */
  "description": string;
  /** url */
  "url": string;
  /** 缩略图url */
  "thumbUrl": string;
  /** 对谁可见（传微信id,多个用,分隔） */
  "groupUser"?: string;
  /** 对谁不可见（传微信id,多个用,分隔） */
  "blackList"?: string;
  /** 对谁可见（传标签id,多个用,分隔） */
  "groupUserLabelIds"?: string;
  /** 对谁不可见（传标签id,多个用,分隔） */
  "blackListLabelIds"?: string;
  [k: string]: unknown;
};

export type Params_peng_you_quan_snsSetAsPrivacy = {
  /** 登录实例标识 */
  "wId": string;
  /** 朋友圈id */
  "id": string;
  [k: string]: unknown;
};

export type Params_peng_you_quan_snsSetPublic = {
  /** 登录实例标识 */
  "wId": string;
  /** 朋友圈id */
  "id": string;
  [k: string]: unknown;
};

export type Params_qun_cao_zuo_acceptMemberGroup = {
  /** 登录实例标识 */
  "wId": string;
  /** 原始 url，好友发送的入群邀请卡片信息链接(回调中取) */
  "url": string;
  [k: string]: unknown;
};

export type Params_qun_cao_zuo_addChatRoomMemberVerify = {
  /** 登录实例标识 */
  "wId": string;
  /** 群id */
  "chatRoomId": string;
  /** 邀请好友的id */
  "userList": unknown;
  /** 邀请理由（管理员查看，不得为空） */
  "reason": string;
  [k: string]: unknown;
};

export type Params_qun_cao_zuo_addGroupMember = {
  /** 登录实例标识 */
  "wId": string;
  /** 群号 */
  "chatRoomId": string;
  /** 群成员微信id，多个已 "," 分割 */
  "userList": string;
  [k: string]: unknown;
};

export type Params_qun_cao_zuo_addRoomMemberFriend = {
  /** 登录实例标识 */
  "wId": string;
  /** 群id */
  "chatRoomId": string;
  /** 群成员的wcId */
  "memberWcId": string;
  /** 申请消息 */
  "content"?: string;
  [k: string]: unknown;
};

export type Params_qun_cao_zuo_agreeAddChatRoomMember = {
  /** 登录实例标识 */
  "wId": string;
  /** 群id */
  "chatRoomId": string;
  /** 入群邀请回调返回newmMsgid */
  "newMsgId": unknown;
  /** 入群邀请的回调xml */
  "xml": string;
  [k: string]: unknown;
};

export type Params_qun_cao_zuo_chuang_jian_wei_xin_qun = {
  /** 登录实例标识 */
  "wId": string;
  /** 群成员微信id，多个已 "," 分割，（必须传输2个微信id以上才可创建群聊） */
  "userList": string;
  /** 群名 */
  "topic"?: string;
  [k: string]: unknown;
};

export type Params_qun_cao_zuo_delGroupMember = {
  /** 登录实例标识 */
  "wId": string;
  /** 群号 */
  "chatRoomId": string;
  /** 群成员微信id，多个已 "," 分割 */
  "userList": string;
  [k: string]: unknown;
};

export type Params_qun_cao_zuo_inviteGroupMember = {
  /** 登录实例标识 */
  "wId": string;
  /** 群号 */
  "chatRoomId": string;
  /** 群成员微信id，多个已 "," 分割 */
  "userList": string;
  [k: string]: unknown;
};

export type Params_qun_cao_zuo_operateChatRoom = {
  /** 登录实例标识 */
  "wId": string;
  /** 群号 */
  "chatRoomId": string;
  /** 群成员微信id，多个用 "," 分割 */
  "wcId": string;
  /** 1：添加群管理（可添加多个微信id） 2：删除群管理（可删除多个） 3：转让（只能转让一个微信号） */
  "type": number;
  [k: string]: unknown;
};

export type Params_qun_cao_zuo_queryGroupDetail = {
  /** 登录实例标识 */
  "wId": string;
  /** 群号 */
  "chatRoomId": string;
  [k: string]: unknown;
};

export type Params_qun_cao_zuo_queryGroupList = {
  /** 登录实例标识 */
  "wId": string;
  /** 群号 */
  "chatRoomId": string;
  [k: string]: unknown;
};

export type Params_qun_cao_zuo_queryGroupMemberDetail = {
  /** 登录实例标识 */
  "wId": string;
  /** 群号 */
  "chatRoomId": string;
  "userList": string;
  [k: string]: unknown;
};

export type Params_qun_cao_zuo_queryGroupQrCode = {
  /** 登录实例标识 */
  "wId": string;
  /** 群号 */
  "chatRoomId": string;
  [k: string]: unknown;
};

export type Params_qun_cao_zuo_quitGroup = {
  /** 登录实例标识 */
  "wId": string;
  /** 群id */
  "chatRoomId": string;
  [k: string]: unknown;
};

export type Params_qun_cao_zuo_roomAppTodo = {
  /** 登录实例标识 */
  "wId": string;
  /** 群id */
  "chatRoomId": string;
  /** 小程序的消息id(小程序消息回调返回newMsgid) */
  "newMsgId": number;
  /** 小程序消息回调中取 */
  "title": unknown;
  /** 小程序消息回调中取 */
  "pagePath": unknown;
  /** 小程序回调中取 */
  "userName": unknown;
  /** 小程序回调中取 */
  "sendWcId": unknown;
  /** 撤回传，设置待办成功后返回本字段 */
  "sign"?: number;
  [k: string]: unknown;
};

export type Params_qun_cao_zuo_roomTodo = {
  /** 登录实例标识 */
  "wId": string;
  /** 群id */
  "chatRoomId": string;
  /** 群公告的消息id(设置群公告成功后，回调返回newMsgId) */
  "newMsgId": number;
  /** 0:设置群待办 1:撤回群待办 */
  "operType": number;
  /** 撤回传，设置待办成功后返回本字段 */
  "sign"?: number;
  [k: string]: unknown;
};

export type Params_qun_cao_zuo_saveGroup = {
  /** 登录实例标识 */
  "wId": string;
  /** 群号 */
  "chatRoomId": string;
  "flag": number;
  [k: string]: unknown;
};

export type Params_qun_cao_zuo_scanJoinRoom = {
  /** 登录实例标识 */
  "wId": string;
  /** 群二维码url（二维码解析后的url） */
  "url": string;
  "type"?: number;
  [k: string]: unknown;
};

export type Params_qun_cao_zuo_setGroupAnnounct = {
  /** 登录实例标识 */
  "wId": string;
  /** 群号 */
  "chatRoomId": string;
  /** 内容 */
  "content": string;
  [k: string]: unknown;
};

export type Params_qun_cao_zuo_updateGroupName = {
  /** 登录实例标识 */
  "wId": string;
  /** 群号 */
  "chatRoomId": string;
  /** 群名 */
  "content": string;
  [k: string]: unknown;
};

export type Params_qun_cao_zuo_updateGroupRemark = {
  /** 登录实例标识 */
  "wId": string;
  /** 群号 */
  "chatRoomId": string;
  /** 群名 */
  "content": string;
  [k: string]: unknown;
};

export type Params_qun_cao_zuo_updateIInChatRoomNickName = {
  /** 登录实例标识 */
  "wId": string;
  [k: string]: unknown;
};

export type Params_shipinhao_createFinder = {
  /** 登录实例标识 */
  "wId": string;
  /** 视频号名称 */
  "nickName": string;
  /** 视频号头像 */
  "headImgUrl": string;
  [k: string]: unknown;
};

export type Params_shipinhao_finderBrowse = {
  /** 登录实例标识 */
  "wId": string;
  /** 视频号作品id */
  "objectId": number;
  /** 视频号作品nonceId */
  "objectNonceId": string;
  /** 通过获取用户主页返回的sessionBuffer */
  "sessionBuffer": string;
  /** 自己的用户编码（获取个人主页接口返回的userName） */
  "myUserName": string;
  /** 自己的角色类型，根据角色关注（获取个人主页接口返回的roleType） */
  "myRoleType": number;
  [k: string]: unknown;
};

export type Params_shipinhao_finderComment = {
  /** 登录实例标识 */
  "wId": string;
  /** 评论内容 */
  "content": string;
  /** 视频号作品id */
  "id": number;
  /** 视频号作品nonceId */
  "objectNonceId": string;
  "type": number;
  [k: string]: unknown;
};

export type Params_shipinhao_finderCommentDetails = {
  /** 登录实例标识 */
  "wId": string;
  /** 视频号作品id */
  "id": number;
  /** 分页code，首次传空，后续传接口返回的 */
  "pageCode": string;
  /** 通过获取用户主页返回的sessionBuffer */
  "sessionBuffer": string;
  /** 默认为0 */
  "refCommentId": string;
  /** 获取评论的回复详情时传上级评论的ID */
  "rootCommentId": number;
  /** 视频号作品nonceId */
  "nonceId": string;
  [k: string]: unknown;
};

export type Params_shipinhao_finderFollow = {
  /** 登录实例标识 */
  "wId": string;
  /** 视频号用户的编码（搜索接口返回的userName） */
  "userName": string;
  /** 自己的用户编码（获取个人主页接口返回的userName） */
  "meUserName": string;
  /** 自己的角色类型，根据角色关注（获取个人主页接口返回的roleType） */
  "meRoleType": number;
  "type": number;
  [k: string]: unknown;
};

export type Params_shipinhao_finderHome = {
  /** 登录实例标识 */
  "wId": string;
  [k: string]: unknown;
};

export type Params_shipinhao_finderIdFav = {
  /** 登录实例标识 */
  "wId": string;
  /** 视频号作品id */
  "id": number;
  /** 视频号作品nonceId */
  "nonceId": string;
  "type": number;
  [k: string]: unknown;
};

export type Params_shipinhao_finderIdLike = {
  /** 登录实例标识 */
  "wId": string;
  /** 视频号作品id */
  "id": number;
  /** 评论的id */
  "commentId": number;
  /** 视频号作品nonceId */
  "nonceId": string;
  /** 视频操作 3喜欢 4不喜欢 评论操作 1喜欢 2不喜欢 */
  "type": number;
  /** 通过获取用户主页返回的sessionBuffer */
  "sessionBuffer": string;
  /** 作者的username */
  "toUserName": string;
  /** 自己的用户编码（获取个人主页接口返回的userName） */
  "meUserName": string;
  /** 自己的角色类型，根据角色关注（获取个人主页接口返回的roleType） */
  "meRoleType": number;
  [k: string]: unknown;
};

export type Params_shipinhao_finderPublish = {
  /** 登录实例标识 */
  "wId": string;
  /** 自己的用户编码 */
  "myUserName": string;
  /** 视频链接 */
  "videoUrl": string;
  /** 封面链接 */
  "videoThumbUrl": string;
  /** 视频宽度 */
  "videoWidth": number;
  /** 视频高度 */
  "videoHeight": number;
  /** 视频播放时长，单位秒 */
  "videoPlayLen": number;
  /** 标题 */
  "title": string;
  /** 话题 */
  "topic": string;
  /** 通过“上传视频号视频”接口获取 */
  "videoCdn"?: Record<string, unknown>;
  /** 通过“上传视频号视频”接口获取 */
  "videoCdn.fileUrl": string;
  /** 通过“上传视频号视频”接口获取 */
  "videoCdn.thumbUrl": string;
  /** 通过“上传视频号视频”接口获取 */
  "videoCdn.mp4Identify": string;
  /** 通过“上传视频号视频”接口获取 */
  "videoCdn.fileSize": number;
  /** 通过“上传视频号视频”接口获取 */
  "videoCdn.thumbMd5": string;
  /** 通过“上传视频号视频”接口获取 */
  "videoCdn.fileKey": string;
  [k: string]: unknown;
};

export type Params_shipinhao_finderUpload = {
  /** 登录实例标识 */
  "wId": string;
  /** 视频链接 */
  "videoUrl": string;
  /** 视频封面图片链接 */
  "imgUrl": string;
  [k: string]: unknown;
};

export type Params_shipinhao_finderUserHome = {
  /** 登录实例标识 */
  "wId": string;
  /** 视频号用户的编码（搜索接口返回的userName） */
  "userName": string;
  /** 分页参数，首次传空，获取下一页时传响应中返回的pageCode */
  "pageCode"?: string;
  [k: string]: unknown;
};

export type Params_shipinhao_getContactDetails = {
  /** 登录实例标识 */
  "wId": string;
  /** 自己的用户编码（获取个人主页接口返回的userName） */
  "meUserName": string;
  /** 自己的角色类型，根据角色关注（获取个人主页接口返回的roleType） */
  "meRoleType": number;
  /** 私信联系人的username */
  "contactUserName": string;
  [k: string]: unknown;
};

export type Params_shipinhao_getFollowList = {
  /** 登录实例标识 */
  "wId": string;
  /** 首次传空，后续传接口返回的lastBuffer */
  "lastBuff"?: string;
  /** 自己的用户编码（获取个人主页接口返回的userName） */
  "myUserName": string;
  /** 自己的角色类型，根据角色关注（获取个人主页接口返回的roleType） */
  "myRoleType": number;
  [k: string]: unknown;
};

export type Params_shipinhao_getLikeAndFavList = {
  /** 登录实例标识 */
  "wId": string;
  /** 首次传空，后续传接口返回的lastBuffer */
  "lastBuffer"?: string;
  /** 自己的用户编码（获取个人主页接口返回的userName） */
  "myUserName": string;
  /** 自己的角色类型，根据角色关注（获取个人主页接口返回的roleType） */
  "myRoleType": number;
  /** 视频类型，7:全部 1:红心 2:大拇指 4:收藏 */
  "flag": number;
  [k: string]: unknown;
};

export type Params_shipinhao_getMentionList = {
  /** 登录实例标识 */
  "wId": string;
  /** 翻页的key，首次传空，翻页传接口返回的lastBuff */
  "lastBuff": string;
  /** 自己的用户编码（获取个人主页接口返回的userName） */
  "myUserName": string;
  /** 自己的角色类型，根据角色关注（获取个人主页接口返回的roleType） */
  "myRoleType": number;
  /** 消息类型，3是点赞 4是评论 5是关注 */
  "reqScene": number;
  [k: string]: unknown;
};

export type Params_shipinhao_getQrCode = {
  /** 登录实例标识 */
  "wId": string;
  /** 自己的用户编码（获取个人主页接口返回的userName） */
  "meUserName": string;
  /** 自己的角色类型，根据角色关注（获取个人主页接口返回的roleType） */
  "meRoleType": number;
  [k: string]: unknown;
};

export type Params_shipinhao_getSessionId = {
  /** 登录实例标识 */
  "wId": string;
  /** 视频号用户的编码 */
  "toUserName": string;
  /** 自己的用户编码 */
  "myUserName": string;
  "type": number;
  [k: string]: unknown;
};

export type Params_shipinhao_modFinderProfile = {
  /** 登录实例标识 */
  "wId": string;
  /** 自己的用户编码（获取个人主页接口返回的userName） */
  "meUserName": string;
  /** 自己的角色类型，根据角色关注（获取个人主页接口返回的roleType） */
  "meRoleType": number;
  /** 视频号昵称 */
  "nickName": string;
  /** 视频号简介 */
  "signature": string;
  /** 视频号头像链接 */
  "headImgUrl": string;
  /** 国家 */
  "country": string;
  /** 省份 */
  "province": string;
  /** 城市 */
  "city": string;
  /** 性别 */
  "sex": number;
  [k: string]: unknown;
};

export type Params_shipinhao_privateSend = {
  /** 登录实例标识 */
  "wId": string;
  /** 视频号用户的编码 */
  "toUserName": string;
  /** 当前微信的用户编码 */
  "myUserName": string;
  /** 通过/getSessionId接口获取 */
  "sessionId": string;
  /** 私信内容 */
  "content": string;
  [k: string]: unknown;
};

export type Params_shipinhao_privateSendImg = {
  /** 登录实例标识 */
  "wId": string;
  /** 视频号用户的编码 */
  "toUserName": string;
  /** 通过/getSessionId接口获取 */
  "sessionId": string;
  /** 图片地址 */
  "imgUrl": string;
  [k: string]: unknown;
};

export type Params_shipinhao_scanFinderHelper = {
  /** 登录实例标识 */
  "wId": string;
  /** 视频号助手官方二维码解析的地址（[二维码](https://channels.weixin.qq.com/login.html)） */
  "url": string;
  [k: string]: unknown;
};

export type Params_shipinhao_searchFinder = {
  /** 登录实例标识 */
  "wId": string;
  /** 搜索内容 */
  "content": string;
  "type": number;
  [k: string]: unknown;
};

export type Params_shou_cang_jia_huo_qu_shou_cang_jia_lie_biao = {
  /** 微信实列ID */
  "wId": string;
  /** 第一次传null,如果接口返回keyBuf第二次传keyBuf */
  "keyBuf": unknown;
  [k: string]: unknown;
};

export type Params_shou_cang_jia_huo_qu_shou_cang_jia_nei_rong = {
  /** 微信实列ID */
  "wId": string;
  /** 收藏标识 */
  "favId": number;
  [k: string]: unknown;
};

export type Params_shou_cang_jia_shan_chu_shou_cang_jia_nei_rong = {
  /** 微信实列ID */
  "wId": string;
  /** 收藏标识 */
  "favId": number;
  [k: string]: unknown;
};

export type Params_te_shu_cdnDownFile = {
  /** 登录实例标识 */
  "wId": string;
  /** XML获取 */
  "cdnUrl": string;
  /** XML获取 */
  "aeskey": string;
  "fileType": number;
  [k: string]: unknown;
};

export type Params_te_shu_getReqTimes = {
  /** 1000成功，1001失败 */
  "code"?: string;
  /** 反馈信息 */
  "msg"?: string;
  /** 开发者账号 */
  "account"?: string;
  /** 微信昵称 */
  "nickName"?: string;
  /** 微信id */
  "wcId"?: string;
  /** 接口调用次数 */
  "times"?: string;
  /** 登录实例标识 */
  "wid"?: string;
  [k: string]: unknown;
};

export type Params_te_shu_getUserFlow = {
  /** 1000成功，1001失败 */
  "code"?: string;
  /** 反馈信息 */
  "msg"?: string;
  /** 开发者账号 */
  "account"?: string;
  /** 微信昵称 */
  "nickName"?: string;
  /** 微信id */
  "wcId"?: string;
  /** 使用流量 */
  "flow"?: string;
  /** 使用流量（以B为单位） */
  "size"?: number;
  /** 登录实例标识 */
  "wid"?: string;
  [k: string]: unknown;
};

export type Params_te_shu_offlineReason = {
  /** 微信id */
  "wcId": string;
  [k: string]: unknown;
};

export type Params_te_shu_sendCdnVideo = {
  /** 登录实例标识 */
  "wId": string;
  /** 视频url链接 */
  "path": string;
  /** 图片url链接 */
  "thumbPath": string;
  [k: string]: unknown;
};

export type Params_te_shu_setproxy = {
  /** 微信id */
  "wcId": string;
  /** 代理IP+端口 */
  "proxyIp": string;
  /** 代理IP平台账号 */
  "proxyUser": string;
  /** 代理IP平台密码 */
  "proxyPassword": string;
  [k: string]: unknown;
};

export type Params_te_shu_uploadCdnImage = {
  /** 登录实例标识 */
  "wId": string;
  /** 图片url链接 */
  "content": string;
  [k: string]: unknown;
};

export type Params_wei_xin_guan_li_cha_xun_wei_xin_shi_fou_zai_xian = {
  /** 登录实例标识 */
  "wId": string;
  [k: string]: unknown;
};

export type Params_wei_xin_guan_li_duan_xian_chong_lian = {
  /** 1000成功（在线），1001失败（离线） */
  "code"?: string;
  /** 反馈信息 */
  "message"?: string;
  /** 微信id */
  "wcId"?: string;
  /** 登录实例标识 */
  "wId"?: string;
  [k: string]: unknown;
};

export type Params_wei_xin_guan_li_pi_liang_xia_xian_wei_xin_hao = {
  /** 账号 */
  "account": string;
  /** 须下线的微信id */
  "wcIds": unknown[];
  [k: string]: unknown;
};

export type Params_xiao_xi_fa_song_fa_song_emoji = {
  /** 登录实例标识 */
  "wId": string;
  /** 接收人微信id/群id */
  "wcId": string;
  /** 取回调中xml中md5字段值 */
  "imageMd5": string;
  /** 取回调中xml中len字段值 */
  "imgSize": string;
  [k: string]: unknown;
};

export type Params_xiao_xi_fa_song_fa_song_lian_jie_xiao_xi = {
  /** 登录实例标识 */
  "wId": string;
  /** 接收人微信id/群id */
  "wcId": string;
  /** 标题 */
  "title": string;
  /** 链接 */
  "url": string;
  /** 描述 */
  "description": string;
  /** 图标url（JPG/PNG格式,50K以内） */
  "thumbUrl": string;
  [k: string]: unknown;
};

export type Params_xiao_xi_fa_song_fa_song_ming_pian_xiao_xi = {
  /** 登录实例标识 */
  "wId": string;
  /** 接收人微信id/群id */
  "wcId": string;
  /** 要发送的名片微信id */
  "nameCardId": string;
  [k: string]: unknown;
};

export type Params_xiao_xi_fa_song_fa_song_shi_pin_xiao_xi = {
  /** 登录实例标识 */
  "wId": string;
  /** 接收人微信id/群id */
  "wcId": string;
  /** 视频url链接 */
  "path": string;
  /** 视频封面url链接（50KB以内） */
  "thumbPath": string;
  [k: string]: unknown;
};

export type Params_xiao_xi_fa_song_fa_song_tu_pian_xiao_xi2 = {
  /** 登录实例标识 */
  "wId": string;
  /** 接收人微信id/群id */
  "wcId": string;
  /** 图片url链接 */
  "content": string;
  [k: string]: unknown;
};

export type Params_xiao_xi_fa_song_fa_song_wen_ben_xiao_xi = {
  /** 登录实例标识 */
  "wId": string;
  /** 接收人微信id/群id */
  "wcId": string;
  /** 文本内容消息 */
  "content": string;
  [k: string]: unknown;
};

export type Params_xiao_xi_fa_song_fa_song_yi_jing_shou_dao_de_shi_pin_xiao_xi = {
  /** 登录实例标识 */
  "wId": string;
  /** 接收人微信id/群号id */
  "wcId": string;
  /** xml视频内容 */
  "content": string;
  [k: string]: unknown;
};

export type Params_xiao_xi_fa_song_fa_song_yi_jing_shou_dao_de_tu_pian_xiao_xi = {
  /** 登录实例标识 */
  "wId": string;
  /** 接收人微信id/群id */
  "wcId": string;
  /** xml图片内容 */
  "content": string;
  [k: string]: unknown;
};

export type Params_xiao_xi_fa_song_fa_song_yi_jing_shou_dao_de_wen_jian = {
  /** 登录实例标识 */
  "wId": string;
  /** 接收人微信id/群id */
  "wcId": string;
  /** xml文件内容 */
  "content": string;
  [k: string]: unknown;
};

export type Params_xiao_xi_fa_song_fa_song_yu_yin_xiao_xi_ji_jiang_kai_fang = {
  /** 登录实例标识 */
  "wId": string;
  /** 接收人微信id/群id */
  "wcId": string;
  /** 语音url （silk/amr 格式,可以[下载消息中的语音返回silk格式](../xiao-xi-jie-shou/xia-zai-xiao-xi-nei-rong/xia-zai-yu-yin-ji-jiang-kai-fang.html)） */
  "content": string;
  /** 语音时长（回调消息xml数据中的voicelength字段） */
  "length": number;
  [k: string]: unknown;
};

export type Params_xiao_xi_fa_song_forwardUrl = {
  /** 登录实例标识 */
  "wId": string;
  /** 接收人微信id/群id */
  "wcId": string;
  /** xml文件内容 */
  "content": string;
  [k: string]: unknown;
};

export type Params_xiao_xi_fa_song_qun_liao_at = {
  /** 登录实例标识 */
  "wId": string;
  /** 接收方群id */
  "wcId": string;
  /** 文本内容消息（@的微信昵称需要自己拼接，必须拼接艾特符号，不然不生效） */
  "content": string;
  "at": string;
  [k: string]: unknown;
};

export type Params_xiao_xi_fa_song_revokeMsg = {
  /** 登录实例标识 */
  "wId": string;
  /** 接收方微信id/群id */
  "wcId": string;
  /** 消息msgId(发送类接口返回的msgId) */
  "msgId": number;
  /** 消息newMsgId(发送类接口返回的newMsgId) */
  "newMsgId": number;
  /** 发送时间 */
  "createTime": number;
  [k: string]: unknown;
};

export type Params_xiao_xi_fa_song_sendApp = {
  /** 登录实例标识 */
  "wId": string;
  /** 接收方微信id/群id */
  "wcId": string;
  /** 消息xml回调内容, (此回调的XML需要去掉部分，截取appmsg开头的，具体请看请求参数示例） */
  "content": string;
  [k: string]: unknown;
};

export type Params_xiao_xi_fa_song_sendApplet = {
  /** 登录实例标识 */
  "wId": string;
  /** 接收方微信id/群id */
  "wcId": string;
  "imgUrl": string;
  [k: string]: unknown;
};

export type Params_xiao_xi_fa_song_sendApplets = {
  /** 登录实例标识 */
  "wId": string;
  /** 接收方微信id/群id */
  "wcId": string;
  /** 小程序的名称，例如：京东 */
  "displayName": string;
  "iconUrl": string;
  [k: string]: unknown;
};

export type Params_xiao_xi_fa_song_sendFile = {
  /** 登录实例标识 */
  "wId": string;
  /** 接收方微信id/群id */
  "wcId": string;
  /** 文件url链接 */
  "path": string;
  /** 文件名 */
  "fileName": string;
  [k: string]: unknown;
};

export type Params_xiao_xi_fa_song_sendFileBase64 = {
  /** 登录实例标识 */
  "wId": string;
  /** 接收方微信id/群id */
  "wcId": string;
  /** 文件名 */
  "fileName": string;
  "base64": string;
  [k: string]: unknown;
};

export type Params_xiao_xi_jie_shou_asynGetMsgVideo = {
  /** 登录实例标识 (包含此参数 所有参数都是从消息回调中取） */
  "wId": string;
  /** 消息id */
  "msgId": number;
  /** 收到的消息的xml数据 */
  "content": string;
  [k: string]: unknown;
};

export type Params_xiao_xi_jie_shou_getMsgEmoji = {
  /** 登录实例标识 */
  "wId": string;
  /** 消息回调中返回 */
  "msgId": number;
  /** 消息回调中返回，收到的emoji消息的xml数据 */
  "content": string;
  [k: string]: unknown;
};

export type Params_xiao_xi_jie_shou_getMsgVideoRes = {
  /** [异步下载视频接口](asynGetMsgVideo.html)返回的id */
  "id": string;
  [k: string]: unknown;
};

export type Params_xiao_xi_jie_shou_qu_xiao_xiao_xi_jie_shou = {
  /** 1000成功，1001失败 */
  "code"?: string;
  /** 反馈信息 */
  "msg"?: string;
  [k: string]: unknown;
};

export type Params_xiao_xi_jie_shou_she_zhi_http_hui_tiao_di_zhi = {
  /** 开发者接口回调地址 */
  "httpUrl": string;
  /** 2:[优化版](callback.html)【PS：建议使用优化版】 */
  "type": number;
  [k: string]: unknown;
};

export type Params_xiao_xi_jie_shou_xia_zai_tu_pian_ji_jiang_kai_fang = {
  /** 登录实例标识 (包含此参数 所有参数都是从消息回调中取） */
  "wId": string;
  /** 消息id */
  "msgId": number;
  /** 收到的消息的xml数据 */
  "content": string;
  "type"?: number;
  [k: string]: unknown;
};

export type Params_xiao_xi_jie_shou_xia_zai_wen_jian_ji_jiang_kai_fang = {
  /** 登录实例标识 包含此参数 所有参数都是从消息回调中取） */
  "wId": string;
  /** 消息id */
  "msgId": number;
  /** 收到的消息的xml数据 */
  "content": string;
  [k: string]: unknown;
};

export type Params_xiao_xi_jie_shou_xia_zai_yu_yin_ji_jiang_kai_fang = {
  /** 登录实例标识 包含此参数 所有参数都是从消息回调中取） */
  "wId": string;
  /** 消息id */
  "msgId": number;
  /** 语音长度（xml数据中的length字段） */
  "length": number;
  /** xml中返回的bufId字段值 */
  "bufId": string;
  /** 发送者 */
  "fromUser": string;
  [k: string]: unknown;
};


export type WkteamApi = {
  biaoQian: {
      addContactLabel: (params: Params_biao_qian_addContactLabel, options?: EndpointCallOptions) => Promise<unknown>;
      delContactLabel: (params: Params_biao_qian_delContactLabel, options?: EndpointCallOptions) => Promise<unknown>;
      getContactLabelList: (params: Params_biao_qian_getContactLabelList, options?: EndpointCallOptions) => Promise<unknown>;
      modifyContactLabel: (params: Params_biao_qian_modifyContactLabel, options?: EndpointCallOptions) => Promise<unknown>;
  };
  dengLu: {
      memberLogin: (params: Params_deng_lu_deng_lu_wei_kong_ping_tai_di_yi_bu, options?: EndpointCallOptions) => Promise<unknown>;
      secondLogin: (params: Params_deng_lu_er_ci_deng_lu, options?: EndpointCallOptions) => Promise<unknown>;
      iPadLogin: (params: Params_deng_lu_huo_qu_wei_xin_er_wei_ma2, options?: EndpointCallOptions) => Promise<unknown>;
      initAddressList: (params: Params_deng_lu_initFriendList, options?: EndpointCallOptions) => Promise<unknown>;
      getAddressList: (params: Params_deng_lu_queryFriendList, options?: EndpointCallOptions) => Promise<unknown>;
      loginByAccountAndPassword: (params: Params_deng_lu_zhang_hao_mi_ma_deng_lu, options?: EndpointCallOptions) => Promise<unknown>;
      getIPadLoginInfo: (params: Params_deng_lu_zhi_xing_wei_xin_deng_lu, options?: EndpointCallOptions) => Promise<unknown>;
  };
  haoYouCaoZuo: {
      acceptUser: (params: Params_hao_you_cao_zuo_acceptUser, options?: EndpointCallOptions) => Promise<unknown>;
      addUser: (params: Params_hao_you_cao_zuo_addFriend, options?: EndpointCallOptions) => Promise<unknown>;
      checkZombie: (params: Params_hao_you_cao_zuo_checkZombie, options?: EndpointCallOptions) => Promise<unknown>;
      getImAddressList: (params: Params_hao_you_cao_zuo_getImAddressList, options?: EndpointCallOptions) => Promise<unknown>;
      getOpenImContact: (params: Params_hao_you_cao_zuo_getOpenImContact, options?: EndpointCallOptions) => Promise<unknown>;
      getQrCode: (params: Params_hao_you_cao_zuo_huo_qu_zi_ji_de_er_wei_ma, options?: EndpointCallOptions) => Promise<unknown>;
      getContact: (params: Params_hao_you_cao_zuo_queryUserInfo, options?: EndpointCallOptions) => Promise<unknown>;
      searchUser: (params: Params_hao_you_cao_zuo_serchUser, options?: EndpointCallOptions) => Promise<unknown>;
      setDisturb: (params: Params_hao_you_cao_zuo_setDisturb, options?: EndpointCallOptions) => Promise<unknown>;
      setFriendPemission: (params: Params_hao_you_cao_zuo_setFriendPermission, options?: EndpointCallOptions) => Promise<unknown>;
      setTop: (params: Params_hao_you_cao_zuo_setTop, options?: EndpointCallOptions) => Promise<unknown>;
      delContact: (params: Params_hao_you_cao_zuo_shan_chu_hao_you, options?: EndpointCallOptions) => Promise<unknown>;
      sendHeadImage: (params: Params_hao_you_cao_zuo_she_zhi_ge_ren_tou_tou_xiang, options?: EndpointCallOptions) => Promise<unknown>;
      userPrivacySettings: (params: Params_hao_you_cao_zuo_userPrivacySettings, options?: EndpointCallOptions) => Promise<unknown>;
      modifyRemark: (params: Params_hao_you_cao_zuo_xiu_gai_hao_you_bei_zhu, options?: EndpointCallOptions) => Promise<unknown>;
  };
  pengYouQuan: {
      asynSnsSendVideo: (params: Params_peng_you_quan_asynSnsSendVideo, options?: EndpointCallOptions) => Promise<unknown>;
      deleteSns: (params: Params_peng_you_quan_deleteSns, options?: EndpointCallOptions) => Promise<unknown>;
      downloadSnsVideo: (params: Params_peng_you_quan_downloadSnsVideo, options?: EndpointCallOptions) => Promise<unknown>;
      forwardSns: (params: Params_peng_you_quan_forwardSns, options?: EndpointCallOptions) => Promise<unknown>;
      getAsynSnsSendVideoRes: (params: Params_peng_you_quan_getAsynSnsSendVideoRes, options?: EndpointCallOptions) => Promise<unknown>;
      getCircle: (params: Params_peng_you_quan_getCircle, options?: EndpointCallOptions) => Promise<unknown>;
      getFriendCircle: (params: Params_peng_you_quan_getFriendCircle, options?: EndpointCallOptions) => Promise<unknown>;
      getSnsObject: (params: Params_peng_you_quan_getSnsObject, options?: EndpointCallOptions) => Promise<unknown>;
      snsCancelPraise: (params: Params_peng_you_quan_snsCancelPraise, options?: EndpointCallOptions) => Promise<unknown>;
      snsComment: (params: Params_peng_you_quan_snsComment, options?: EndpointCallOptions) => Promise<unknown>;
      snsCommentDel: (params: Params_peng_you_quan_snsCommentDel, options?: EndpointCallOptions) => Promise<unknown>;
      snsPraise: (params: Params_peng_you_quan_snsPraise, options?: EndpointCallOptions) => Promise<unknown>;
      snsPrivacySettings: (params: Params_peng_you_quan_snsPrivacySettings, options?: EndpointCallOptions) => Promise<unknown>;
      snsSend: (params: Params_peng_you_quan_snsSend, options?: EndpointCallOptions) => Promise<unknown>;
      snsSendImage: (params: Params_peng_you_quan_snsSendImage, options?: EndpointCallOptions) => Promise<unknown>;
      snsSendUrl: (params: Params_peng_you_quan_snsSendUrl, options?: EndpointCallOptions) => Promise<unknown>;
      snsSetAsPrivacy: (params: Params_peng_you_quan_snsSetAsPrivacy, options?: EndpointCallOptions) => Promise<unknown>;
      snsSetPublic: (params: Params_peng_you_quan_snsSetPublic, options?: EndpointCallOptions) => Promise<unknown>;
  };
  qunCaoZuo: {
      acceptUrl: (params: Params_qun_cao_zuo_acceptMemberGroup, options?: EndpointCallOptions) => Promise<unknown>;
      addChatRoomMemberVerify: (params: Params_qun_cao_zuo_addChatRoomMemberVerify, options?: EndpointCallOptions) => Promise<unknown>;
      addChatRoomMember: (params: Params_qun_cao_zuo_addGroupMember, options?: EndpointCallOptions) => Promise<unknown>;
      addRoomMemberFriend: (params: Params_qun_cao_zuo_addRoomMemberFriend, options?: EndpointCallOptions) => Promise<unknown>;
      agreeAddChatRoomMember: (params: Params_qun_cao_zuo_agreeAddChatRoomMember, options?: EndpointCallOptions) => Promise<unknown>;
      createChatroom: (params: Params_qun_cao_zuo_chuang_jian_wei_xin_qun, options?: EndpointCallOptions) => Promise<unknown>;
      deleteChatRoomMember: (params: Params_qun_cao_zuo_delGroupMember, options?: EndpointCallOptions) => Promise<unknown>;
      inviteChatRoomMember: (params: Params_qun_cao_zuo_inviteGroupMember, options?: EndpointCallOptions) => Promise<unknown>;
      operateChatRoom: (params: Params_qun_cao_zuo_operateChatRoom, options?: EndpointCallOptions) => Promise<unknown>;
      getChatRoomInfo: (params: Params_qun_cao_zuo_queryGroupDetail, options?: EndpointCallOptions) => Promise<unknown>;
      getChatRoomMember: (params: Params_qun_cao_zuo_queryGroupList, options?: EndpointCallOptions) => Promise<unknown>;
      getChatRoomMemberInfo: (params: Params_qun_cao_zuo_queryGroupMemberDetail, options?: EndpointCallOptions) => Promise<unknown>;
      getGroupQrCode: (params: Params_qun_cao_zuo_queryGroupQrCode, options?: EndpointCallOptions) => Promise<unknown>;
      quitChatRoom: (params: Params_qun_cao_zuo_quitGroup, options?: EndpointCallOptions) => Promise<unknown>;
      roomAppTodo: (params: Params_qun_cao_zuo_roomAppTodo, options?: EndpointCallOptions) => Promise<unknown>;
      roomTodo: (params: Params_qun_cao_zuo_roomTodo, options?: EndpointCallOptions) => Promise<unknown>;
      showInAddressBook: (params: Params_qun_cao_zuo_saveGroup, options?: EndpointCallOptions) => Promise<unknown>;
      scanJoinRoom: (params: Params_qun_cao_zuo_scanJoinRoom, options?: EndpointCallOptions) => Promise<unknown>;
      setChatRoomAnnouncement: (params: Params_qun_cao_zuo_setGroupAnnounct, options?: EndpointCallOptions) => Promise<unknown>;
      modifyGroupName: (params: Params_qun_cao_zuo_updateGroupName, options?: EndpointCallOptions) => Promise<unknown>;
      modifyGroupRemark: (params: Params_qun_cao_zuo_updateGroupRemark, options?: EndpointCallOptions) => Promise<unknown>;
      updateIInChatRoomNickName: (params: Params_qun_cao_zuo_updateIInChatRoomNickName, options?: EndpointCallOptions) => Promise<unknown>;
  };
  shipinhao: {
      createFinder: (params: Params_shipinhao_createFinder, options?: EndpointCallOptions) => Promise<unknown>;
      finderBrowse: (params: Params_shipinhao_finderBrowse, options?: EndpointCallOptions) => Promise<unknown>;
      finderComment: (params: Params_shipinhao_finderComment, options?: EndpointCallOptions) => Promise<unknown>;
      finderCommentDetails: (params: Params_shipinhao_finderCommentDetails, options?: EndpointCallOptions) => Promise<unknown>;
      finderFollow: (params: Params_shipinhao_finderFollow, options?: EndpointCallOptions) => Promise<unknown>;
      finderHome: (params: Params_shipinhao_finderHome, options?: EndpointCallOptions) => Promise<unknown>;
      finderIdFav: (params: Params_shipinhao_finderIdFav, options?: EndpointCallOptions) => Promise<unknown>;
      finderIdLike: (params: Params_shipinhao_finderIdLike, options?: EndpointCallOptions) => Promise<unknown>;
      finderPublish: (params: Params_shipinhao_finderPublish, options?: EndpointCallOptions) => Promise<unknown>;
      finderUpload: (params: Params_shipinhao_finderUpload, options?: EndpointCallOptions) => Promise<unknown>;
      finderUserHome: (params: Params_shipinhao_finderUserHome, options?: EndpointCallOptions) => Promise<unknown>;
      finderGetContactDetails: (params: Params_shipinhao_getContactDetails, options?: EndpointCallOptions) => Promise<unknown>;
      finderGetFollowList: (params: Params_shipinhao_getFollowList, options?: EndpointCallOptions) => Promise<unknown>;
      finderGetLikeAndFavList: (params: Params_shipinhao_getLikeAndFavList, options?: EndpointCallOptions) => Promise<unknown>;
      finderGetMentionList: (params: Params_shipinhao_getMentionList, options?: EndpointCallOptions) => Promise<unknown>;
      finderGetQrCode: (params: Params_shipinhao_getQrCode, options?: EndpointCallOptions) => Promise<unknown>;
      getSessionId: (params: Params_shipinhao_getSessionId, options?: EndpointCallOptions) => Promise<unknown>;
      modFinderProfile: (params: Params_shipinhao_modFinderProfile, options?: EndpointCallOptions) => Promise<unknown>;
      privateSend: (params: Params_shipinhao_privateSend, options?: EndpointCallOptions) => Promise<unknown>;
      privateSendImg: (params: Params_shipinhao_privateSendImg, options?: EndpointCallOptions) => Promise<unknown>;
      scanFinderHelper: (params: Params_shipinhao_scanFinderHelper, options?: EndpointCallOptions) => Promise<unknown>;
      newSearchFinder: (params: Params_shipinhao_searchFinder, options?: EndpointCallOptions) => Promise<unknown>;
  };
  shouCangJia: {
      weChatFavoritesFavSync: (params: Params_shou_cang_jia_huo_qu_shou_cang_jia_lie_biao, options?: EndpointCallOptions) => Promise<unknown>;
      weChatFavoritesGetFavItem: (params: Params_shou_cang_jia_huo_qu_shou_cang_jia_nei_rong, options?: EndpointCallOptions) => Promise<unknown>;
      weChatFavoritesDelFavItem: (params: Params_shou_cang_jia_shan_chu_shou_cang_jia_nei_rong, options?: EndpointCallOptions) => Promise<unknown>;
  };
  teShu: {
      cdnDownFile: (params: Params_te_shu_cdnDownFile, options?: EndpointCallOptions) => Promise<unknown>;
      getReqTimes: (params: Params_te_shu_getReqTimes, options?: EndpointCallOptions) => Promise<unknown>;
      getUserFlow: (params: Params_te_shu_getUserFlow, options?: EndpointCallOptions) => Promise<unknown>;
      offlineReason: (params: Params_te_shu_offlineReason, options?: EndpointCallOptions) => Promise<unknown>;
      sendCdnVideo: (params: Params_te_shu_sendCdnVideo, options?: EndpointCallOptions) => Promise<unknown>;
      setproxy: (params: Params_te_shu_setproxy, options?: EndpointCallOptions) => Promise<unknown>;
      uploadCdnImage: (params: Params_te_shu_uploadCdnImage, options?: EndpointCallOptions) => Promise<unknown>;
  };
  weiXinGuanLi: {
      isOnline: (params: Params_wei_xin_guan_li_cha_xun_wei_xin_shi_fou_zai_xian, options?: EndpointCallOptions) => Promise<unknown>;
      queryLoginWx: (params: Params_wei_xin_guan_li_duan_xian_chong_lian, options?: EndpointCallOptions) => Promise<unknown>;
      memberOffline: (params: Params_wei_xin_guan_li_pi_liang_xia_xian_wei_xin_hao, options?: EndpointCallOptions) => Promise<unknown>;
  };
  xiaoXiFaSong: {
      sendEmoji: (params: Params_xiao_xi_fa_song_fa_song_emoji, options?: EndpointCallOptions) => Promise<unknown>;
      sendUrl: (params: Params_xiao_xi_fa_song_fa_song_lian_jie_xiao_xi, options?: EndpointCallOptions) => Promise<unknown>;
      sendNameCard: (params: Params_xiao_xi_fa_song_fa_song_ming_pian_xiao_xi, options?: EndpointCallOptions) => Promise<unknown>;
      sendVideo: (params: Params_xiao_xi_fa_song_fa_song_shi_pin_xiao_xi, options?: EndpointCallOptions) => Promise<unknown>;
      sendImage2: (params: Params_xiao_xi_fa_song_fa_song_tu_pian_xiao_xi2, options?: EndpointCallOptions) => Promise<unknown>;
      xiaoXiFaSongFaSongWenBenXiaoXi: (params: Params_xiao_xi_fa_song_fa_song_wen_ben_xiao_xi, options?: EndpointCallOptions) => Promise<unknown>;
      sendRecvVideo: (params: Params_xiao_xi_fa_song_fa_song_yi_jing_shou_dao_de_shi_pin_xiao_xi, options?: EndpointCallOptions) => Promise<unknown>;
      sendRecvImage: (params: Params_xiao_xi_fa_song_fa_song_yi_jing_shou_dao_de_tu_pian_xiao_xi, options?: EndpointCallOptions) => Promise<unknown>;
      sendRecvFile: (params: Params_xiao_xi_fa_song_fa_song_yi_jing_shou_dao_de_wen_jian, options?: EndpointCallOptions) => Promise<unknown>;
      sendVoice: (params: Params_xiao_xi_fa_song_fa_song_yu_yin_xiao_xi_ji_jiang_kai_fang, options?: EndpointCallOptions) => Promise<unknown>;
      forwardUrl: (params: Params_xiao_xi_fa_song_forwardUrl, options?: EndpointCallOptions) => Promise<unknown>;
      xiaoXiFaSongQunLiaoAt: (params: Params_xiao_xi_fa_song_qun_liao_at, options?: EndpointCallOptions) => Promise<unknown>;
      revokeMsg: (params: Params_xiao_xi_fa_song_revokeMsg, options?: EndpointCallOptions) => Promise<unknown>;
      xiaoXiFaSongSendApp: (params: Params_xiao_xi_fa_song_sendApp, options?: EndpointCallOptions) => Promise<unknown>;
      xiaoXiFaSongSendApplet: (params: Params_xiao_xi_fa_song_sendApplet, options?: EndpointCallOptions) => Promise<unknown>;
      sendApplets: (params: Params_xiao_xi_fa_song_sendApplets, options?: EndpointCallOptions) => Promise<unknown>;
      sendFile: (params: Params_xiao_xi_fa_song_sendFile, options?: EndpointCallOptions) => Promise<unknown>;
      sendFileBase64: (params: Params_xiao_xi_fa_song_sendFileBase64, options?: EndpointCallOptions) => Promise<unknown>;
  };
  xiaoXiJieShou: {
      asynGetMsgVideo: (params: Params_xiao_xi_jie_shou_asynGetMsgVideo, options?: EndpointCallOptions) => Promise<unknown>;
      getMsgEmoji: (params: Params_xiao_xi_jie_shou_getMsgEmoji, options?: EndpointCallOptions) => Promise<unknown>;
      getMsgVideoRes: (params: Params_xiao_xi_jie_shou_getMsgVideoRes, options?: EndpointCallOptions) => Promise<unknown>;
      cancelHttpCallbackUrl: (params: Params_xiao_xi_jie_shou_qu_xiao_xiao_xi_jie_shou, options?: EndpointCallOptions) => Promise<unknown>;
      setHttpCallbackUrl: (params: Params_xiao_xi_jie_shou_she_zhi_http_hui_tiao_di_zhi, options?: EndpointCallOptions) => Promise<unknown>;
      getMsgImg: (params: Params_xiao_xi_jie_shou_xia_zai_tu_pian_ji_jiang_kai_fang, options?: EndpointCallOptions) => Promise<unknown>;
      getMsgFile: (params: Params_xiao_xi_jie_shou_xia_zai_wen_jian_ji_jiang_kai_fang, options?: EndpointCallOptions) => Promise<unknown>;
      getMsgVoice: (params: Params_xiao_xi_jie_shou_xia_zai_yu_yin_ji_jiang_kai_fang, options?: EndpointCallOptions) => Promise<unknown>;
  };
};

export function createApi(client: WkteamCoreClient): WkteamApi {
  return {
    biaoQian: {
      /**
       * 创建标签
       *
       * - method: POST
       * - path: /addContactLabel
       * - doc: docs/api/api-wen-dang2/biao-qian/addContactLabel.md
       */
      addContactLabel: async (params, options) => {
        const parsed = validators["biao_qian_addContactLabel"].safeParse(params ?? {});
        if (!parsed.success) {
          const rid = options?.requestId ?? `validation_${Date.now()}`;
          const issues = parsed.error.issues
            .slice(0, 5)
            .map((i) => `${i.path.join(".") || "<root>"}: ${i.message}`)
            .join("; ");
          throw new WkteamValidationError(`参数校验失败: ${issues}`, {
            requestId: rid,
            operationId: "biao_qian_addContactLabel",
          });
        }
        return client.call({
          operationId: "biao_qian_addContactLabel",
          method: "POST",
          path: "/addContactLabel",
          params: parsed.data,
          requiresAuth: true,
          retryCategory: "side_effect",
          options,
        });
      },

      /**
       * 删除联系人标签
       *
       * - method: POST
       * - path: /delContactLabel
       * - doc: docs/api/api-wen-dang2/biao-qian/delContactLabel.md
       */
      delContactLabel: async (params, options) => {
        const parsed = validators["biao_qian_delContactLabel"].safeParse(params ?? {});
        if (!parsed.success) {
          const rid = options?.requestId ?? `validation_${Date.now()}`;
          const issues = parsed.error.issues
            .slice(0, 5)
            .map((i) => `${i.path.join(".") || "<root>"}: ${i.message}`)
            .join("; ");
          throw new WkteamValidationError(`参数校验失败: ${issues}`, {
            requestId: rid,
            operationId: "biao_qian_delContactLabel",
          });
        }
        return client.call({
          operationId: "biao_qian_delContactLabel",
          method: "POST",
          path: "/delContactLabel",
          params: parsed.data,
          requiresAuth: true,
          retryCategory: "side_effect",
          options,
        });
      },

      /**
       * 获取标签列表
       *
       * - method: POST
       * - path: /getContactLabelList
       * - doc: docs/api/api-wen-dang2/biao-qian/getContactLabelList.md
       */
      getContactLabelList: async (params, options) => {
        const parsed = validators["biao_qian_getContactLabelList"].safeParse(params ?? {});
        if (!parsed.success) {
          const rid = options?.requestId ?? `validation_${Date.now()}`;
          const issues = parsed.error.issues
            .slice(0, 5)
            .map((i) => `${i.path.join(".") || "<root>"}: ${i.message}`)
            .join("; ");
          throw new WkteamValidationError(`参数校验失败: ${issues}`, {
            requestId: rid,
            operationId: "biao_qian_getContactLabelList",
          });
        }
        return client.call({
          operationId: "biao_qian_getContactLabelList",
          method: "POST",
          path: "/getContactLabelList",
          params: parsed.data,
          requiresAuth: true,
          retryCategory: "safe",
          options,
        });
      },

      /**
       * 修改联系人标签
       *
       * - method: POST
       * - path: /modifyContactLabel
       * - doc: docs/api/api-wen-dang2/biao-qian/modifyContactLabel.md
       */
      modifyContactLabel: async (params, options) => {
        const parsed = validators["biao_qian_modifyContactLabel"].safeParse(params ?? {});
        if (!parsed.success) {
          const rid = options?.requestId ?? `validation_${Date.now()}`;
          const issues = parsed.error.issues
            .slice(0, 5)
            .map((i) => `${i.path.join(".") || "<root>"}: ${i.message}`)
            .join("; ");
          throw new WkteamValidationError(`参数校验失败: ${issues}`, {
            requestId: rid,
            operationId: "biao_qian_modifyContactLabel",
          });
        }
        return client.call({
          operationId: "biao_qian_modifyContactLabel",
          method: "POST",
          path: "/modifyContactLabel",
          params: parsed.data,
          requiresAuth: true,
          retryCategory: "side_effect",
          options,
        });
      },
    },
    dengLu: {
      /**
       * 登录E云平台（第一步）
       *
       * - method: POST
       * - path: /member/login
       * - doc: docs/api/api-wen-dang2/deng-lu/deng-lu-wei-kong-ping-tai-di-yi-bu.md
       */
      memberLogin: async (params, options) => {
        const parsed = validators["deng_lu_deng_lu_wei_kong_ping_tai_di_yi_bu"].safeParse(params ?? {});
        if (!parsed.success) {
          const rid = options?.requestId ?? `validation_${Date.now()}`;
          const issues = parsed.error.issues
            .slice(0, 5)
            .map((i) => `${i.path.join(".") || "<root>"}: ${i.message}`)
            .join("; ");
          throw new WkteamValidationError(`参数校验失败: ${issues}`, {
            requestId: rid,
            operationId: "deng_lu_deng_lu_wei_kong_ping_tai_di_yi_bu",
          });
        }
        return client.call({
          operationId: "deng_lu_deng_lu_wei_kong_ping_tai_di_yi_bu",
          method: "POST",
          path: "/member/login",
          params: parsed.data,
          requiresAuth: false,
          retryCategory: "side_effect",
          options,
        });
      },

      /**
       * 弹框登录
       *
       * - method: POST
       * - path: /secondLogin
       * - doc: docs/api/api-wen-dang2/deng-lu/er-ci-deng-lu.md
       */
      secondLogin: async (params, options) => {
        const parsed = validators["deng_lu_er_ci_deng_lu"].safeParse(params ?? {});
        if (!parsed.success) {
          const rid = options?.requestId ?? `validation_${Date.now()}`;
          const issues = parsed.error.issues
            .slice(0, 5)
            .map((i) => `${i.path.join(".") || "<root>"}: ${i.message}`)
            .join("; ");
          throw new WkteamValidationError(`参数校验失败: ${issues}`, {
            requestId: rid,
            operationId: "deng_lu_er_ci_deng_lu",
          });
        }
        return client.call({
          operationId: "deng_lu_er_ci_deng_lu",
          method: "POST",
          path: "/secondLogin",
          params: parsed.data,
          requiresAuth: true,
          retryCategory: "side_effect",
          options,
        });
      },

      /**
       * 获取二维码（第二步-方式1）
       *
       * - method: POST
       * - path: /iPadLogin
       * - doc: docs/api/api-wen-dang2/deng-lu/huo-qu-wei-xin-er-wei-ma2.md
       */
      iPadLogin: async (params, options) => {
        const parsed = validators["deng_lu_huo_qu_wei_xin_er_wei_ma2"].safeParse(params ?? {});
        if (!parsed.success) {
          const rid = options?.requestId ?? `validation_${Date.now()}`;
          const issues = parsed.error.issues
            .slice(0, 5)
            .map((i) => `${i.path.join(".") || "<root>"}: ${i.message}`)
            .join("; ");
          throw new WkteamValidationError(`参数校验失败: ${issues}`, {
            requestId: rid,
            operationId: "deng_lu_huo_qu_wei_xin_er_wei_ma2",
          });
        }
        return client.call({
          operationId: "deng_lu_huo_qu_wei_xin_er_wei_ma2",
          method: "POST",
          path: "/iPadLogin",
          params: parsed.data,
          requiresAuth: false,
          retryCategory: "safe",
          options,
        });
      },

      /**
       * 初始化通讯录列表
       *
       * - method: POST
       * - path: /initAddressList
       * - doc: docs/api/api-wen-dang2/deng-lu/initFriendList.md
       */
      initAddressList: async (params, options) => {
        const parsed = validators["deng_lu_initFriendList"].safeParse(params ?? {});
        if (!parsed.success) {
          const rid = options?.requestId ?? `validation_${Date.now()}`;
          const issues = parsed.error.issues
            .slice(0, 5)
            .map((i) => `${i.path.join(".") || "<root>"}: ${i.message}`)
            .join("; ");
          throw new WkteamValidationError(`参数校验失败: ${issues}`, {
            requestId: rid,
            operationId: "deng_lu_initFriendList",
          });
        }
        return client.call({
          operationId: "deng_lu_initFriendList",
          method: "POST",
          path: "/initAddressList",
          params: parsed.data,
          requiresAuth: true,
          retryCategory: "safe",
          options,
        });
      },

      /**
       * 获取通讯录列表
       *
       * - method: POST
       * - path: /getAddressList
       * - doc: docs/api/api-wen-dang2/deng-lu/queryFriendList.md
       */
      getAddressList: async (params, options) => {
        const parsed = validators["deng_lu_queryFriendList"].safeParse(params ?? {});
        if (!parsed.success) {
          const rid = options?.requestId ?? `validation_${Date.now()}`;
          const issues = parsed.error.issues
            .slice(0, 5)
            .map((i) => `${i.path.join(".") || "<root>"}: ${i.message}`)
            .join("; ");
          throw new WkteamValidationError(`参数校验失败: ${issues}`, {
            requestId: rid,
            operationId: "deng_lu_queryFriendList",
          });
        }
        return client.call({
          operationId: "deng_lu_queryFriendList",
          method: "POST",
          path: "/getAddressList",
          params: parsed.data,
          requiresAuth: true,
          retryCategory: "safe",
          options,
        });
      },

      /**
       * 账号密码登录
       *
       * - method: POST
       * - path: /loginByAccountAndPassword
       * - doc: docs/api/api-wen-dang2/deng-lu/zhang-hao-mi-ma-deng-lu.md
       */
      loginByAccountAndPassword: async (params, options) => {
        const parsed = validators["deng_lu_zhang_hao_mi_ma_deng_lu"].safeParse(params ?? {});
        if (!parsed.success) {
          const rid = options?.requestId ?? `validation_${Date.now()}`;
          const issues = parsed.error.issues
            .slice(0, 5)
            .map((i) => `${i.path.join(".") || "<root>"}: ${i.message}`)
            .join("; ");
          throw new WkteamValidationError(`参数校验失败: ${issues}`, {
            requestId: rid,
            operationId: "deng_lu_zhang_hao_mi_ma_deng_lu",
          });
        }
        return client.call({
          operationId: "deng_lu_zhang_hao_mi_ma_deng_lu",
          method: "POST",
          path: "/loginByAccountAndPassword",
          params: parsed.data,
          requiresAuth: false,
          retryCategory: "side_effect",
          options,
        });
      },

      /**
       * 执行微信登录（第三步）
       *
       * - method: POST
       * - path: /getIPadLoginInfo
       * - doc: docs/api/api-wen-dang2/deng-lu/zhi-xing-wei-xin-deng-lu.md
       */
      getIPadLoginInfo: async (params, options) => {
        const parsed = validators["deng_lu_zhi_xing_wei_xin_deng_lu"].safeParse(params ?? {});
        if (!parsed.success) {
          const rid = options?.requestId ?? `validation_${Date.now()}`;
          const issues = parsed.error.issues
            .slice(0, 5)
            .map((i) => `${i.path.join(".") || "<root>"}: ${i.message}`)
            .join("; ");
          throw new WkteamValidationError(`参数校验失败: ${issues}`, {
            requestId: rid,
            operationId: "deng_lu_zhi_xing_wei_xin_deng_lu",
          });
        }
        return client.call({
          operationId: "deng_lu_zhi_xing_wei_xin_deng_lu",
          method: "POST",
          path: "/getIPadLoginInfo",
          params: parsed.data,
          requiresAuth: false,
          retryCategory: "safe",
          options,
        });
      },
    },
    haoYouCaoZuo: {
      /**
       * 同意添加好友
       *
       * - method: POST
       * - path: /acceptUser
       * - doc: docs/api/api-wen-dang2/hao-you-cao-zuo/acceptUser.md
       */
      acceptUser: async (params, options) => {
        const parsed = validators["hao_you_cao_zuo_acceptUser"].safeParse(params ?? {});
        if (!parsed.success) {
          const rid = options?.requestId ?? `validation_${Date.now()}`;
          const issues = parsed.error.issues
            .slice(0, 5)
            .map((i) => `${i.path.join(".") || "<root>"}: ${i.message}`)
            .join("; ");
          throw new WkteamValidationError(`参数校验失败: ${issues}`, {
            requestId: rid,
            operationId: "hao_you_cao_zuo_acceptUser",
          });
        }
        return client.call({
          operationId: "hao_you_cao_zuo_acceptUser",
          method: "POST",
          path: "/acceptUser",
          params: parsed.data,
          requiresAuth: true,
          retryCategory: "side_effect",
          options,
        });
      },

      /**
       * 添加好友
       *
       * - method: POST
       * - path: /addUser
       * - doc: docs/api/api-wen-dang2/hao-you-cao-zuo/addFriend.md
       */
      addUser: async (params, options) => {
        const parsed = validators["hao_you_cao_zuo_addFriend"].safeParse(params ?? {});
        if (!parsed.success) {
          const rid = options?.requestId ?? `validation_${Date.now()}`;
          const issues = parsed.error.issues
            .slice(0, 5)
            .map((i) => `${i.path.join(".") || "<root>"}: ${i.message}`)
            .join("; ");
          throw new WkteamValidationError(`参数校验失败: ${issues}`, {
            requestId: rid,
            operationId: "hao_you_cao_zuo_addFriend",
          });
        }
        return client.call({
          operationId: "hao_you_cao_zuo_addFriend",
          method: "POST",
          path: "/addUser",
          params: parsed.data,
          requiresAuth: true,
          retryCategory: "side_effect",
          options,
        });
      },

      /**
       * 检测好友状态
       *
       * - method: POST
       * - path: /checkZombie
       * - doc: docs/api/api-wen-dang2/hao-you-cao-zuo/checkZombie.md
       */
      checkZombie: async (params, options) => {
        const parsed = validators["hao_you_cao_zuo_checkZombie"].safeParse(params ?? {});
        if (!parsed.success) {
          const rid = options?.requestId ?? `validation_${Date.now()}`;
          const issues = parsed.error.issues
            .slice(0, 5)
            .map((i) => `${i.path.join(".") || "<root>"}: ${i.message}`)
            .join("; ");
          throw new WkteamValidationError(`参数校验失败: ${issues}`, {
            requestId: rid,
            operationId: "hao_you_cao_zuo_checkZombie",
          });
        }
        return client.call({
          operationId: "hao_you_cao_zuo_checkZombie",
          method: "POST",
          path: "/checkZombie",
          params: parsed.data,
          requiresAuth: true,
          retryCategory: "side_effect",
          options,
        });
      },

      /**
       * 获取企微联系人列表
       *
       * - method: POST
       * - path: /getImAddressList
       * - doc: docs/api/api-wen-dang2/hao-you-cao-zuo/getImAddressList.md
       */
      getImAddressList: async (params, options) => {
        const parsed = validators["hao_you_cao_zuo_getImAddressList"].safeParse(params ?? {});
        if (!parsed.success) {
          const rid = options?.requestId ?? `validation_${Date.now()}`;
          const issues = parsed.error.issues
            .slice(0, 5)
            .map((i) => `${i.path.join(".") || "<root>"}: ${i.message}`)
            .join("; ");
          throw new WkteamValidationError(`参数校验失败: ${issues}`, {
            requestId: rid,
            operationId: "hao_you_cao_zuo_getImAddressList",
          });
        }
        return client.call({
          operationId: "hao_you_cao_zuo_getImAddressList",
          method: "POST",
          path: "/getImAddressList",
          params: parsed.data,
          requiresAuth: true,
          retryCategory: "safe",
          options,
        });
      },

      /**
       * 获取企微联系人信息
       *
       * - method: POST
       * - path: /getOpenImContact
       * - doc: docs/api/api-wen-dang2/hao-you-cao-zuo/getOpenImContact.md
       */
      getOpenImContact: async (params, options) => {
        const parsed = validators["hao_you_cao_zuo_getOpenImContact"].safeParse(params ?? {});
        if (!parsed.success) {
          const rid = options?.requestId ?? `validation_${Date.now()}`;
          const issues = parsed.error.issues
            .slice(0, 5)
            .map((i) => `${i.path.join(".") || "<root>"}: ${i.message}`)
            .join("; ");
          throw new WkteamValidationError(`参数校验失败: ${issues}`, {
            requestId: rid,
            operationId: "hao_you_cao_zuo_getOpenImContact",
          });
        }
        return client.call({
          operationId: "hao_you_cao_zuo_getOpenImContact",
          method: "POST",
          path: "/getOpenImContact",
          params: parsed.data,
          requiresAuth: true,
          retryCategory: "safe",
          options,
        });
      },

      /**
       * 获取我的二维码
       *
       * - method: POST
       * - path: /getQrCode
       * - doc: docs/api/api-wen-dang2/hao-you-cao-zuo/huo-qu-zi-ji-de-er-wei-ma.md
       */
      getQrCode: async (params, options) => {
        const parsed = validators["hao_you_cao_zuo_huo_qu_zi_ji_de_er_wei_ma"].safeParse(params ?? {});
        if (!parsed.success) {
          const rid = options?.requestId ?? `validation_${Date.now()}`;
          const issues = parsed.error.issues
            .slice(0, 5)
            .map((i) => `${i.path.join(".") || "<root>"}: ${i.message}`)
            .join("; ");
          throw new WkteamValidationError(`参数校验失败: ${issues}`, {
            requestId: rid,
            operationId: "hao_you_cao_zuo_huo_qu_zi_ji_de_er_wei_ma",
          });
        }
        return client.call({
          operationId: "hao_you_cao_zuo_huo_qu_zi_ji_de_er_wei_ma",
          method: "POST",
          path: "/getQrCode",
          params: parsed.data,
          requiresAuth: true,
          retryCategory: "safe",
          options,
        });
      },

      /**
       * 获取联系人信息
       *
       * - method: POST
       * - path: /getContact
       * - doc: docs/api/api-wen-dang2/hao-you-cao-zuo/queryUserInfo.md
       */
      getContact: async (params, options) => {
        const parsed = validators["hao_you_cao_zuo_queryUserInfo"].safeParse(params ?? {});
        if (!parsed.success) {
          const rid = options?.requestId ?? `validation_${Date.now()}`;
          const issues = parsed.error.issues
            .slice(0, 5)
            .map((i) => `${i.path.join(".") || "<root>"}: ${i.message}`)
            .join("; ");
          throw new WkteamValidationError(`参数校验失败: ${issues}`, {
            requestId: rid,
            operationId: "hao_you_cao_zuo_queryUserInfo",
          });
        }
        return client.call({
          operationId: "hao_you_cao_zuo_queryUserInfo",
          method: "POST",
          path: "/getContact",
          params: parsed.data,
          requiresAuth: true,
          retryCategory: "safe",
          options,
        });
      },

      /**
       * 搜索联系人
       *
       * - method: POST
       * - path: /searchUser
       * - doc: docs/api/api-wen-dang2/hao-you-cao-zuo/serchUser.md
       */
      searchUser: async (params, options) => {
        const parsed = validators["hao_you_cao_zuo_serchUser"].safeParse(params ?? {});
        if (!parsed.success) {
          const rid = options?.requestId ?? `validation_${Date.now()}`;
          const issues = parsed.error.issues
            .slice(0, 5)
            .map((i) => `${i.path.join(".") || "<root>"}: ${i.message}`)
            .join("; ");
          throw new WkteamValidationError(`参数校验失败: ${issues}`, {
            requestId: rid,
            operationId: "hao_you_cao_zuo_serchUser",
          });
        }
        return client.call({
          operationId: "hao_you_cao_zuo_serchUser",
          method: "POST",
          path: "/searchUser",
          params: parsed.data,
          requiresAuth: true,
          retryCategory: "side_effect",
          options,
        });
      },

      /**
       * 设置聊天免打扰
       *
       * - method: POST
       * - path: /setDisturb
       * - doc: docs/api/api-wen-dang2/hao-you-cao-zuo/setDisturb.md
       */
      setDisturb: async (params, options) => {
        const parsed = validators["hao_you_cao_zuo_setDisturb"].safeParse(params ?? {});
        if (!parsed.success) {
          const rid = options?.requestId ?? `validation_${Date.now()}`;
          const issues = parsed.error.issues
            .slice(0, 5)
            .map((i) => `${i.path.join(".") || "<root>"}: ${i.message}`)
            .join("; ");
          throw new WkteamValidationError(`参数校验失败: ${issues}`, {
            requestId: rid,
            operationId: "hao_you_cao_zuo_setDisturb",
          });
        }
        return client.call({
          operationId: "hao_you_cao_zuo_setDisturb",
          method: "POST",
          path: "/setDisturb",
          params: parsed.data,
          requiresAuth: true,
          retryCategory: "side_effect",
          options,
        });
      },

      /**
       * 设置好友权限
       *
       * - method: POST
       * - path: /setFriendPemission
       * - doc: docs/api/api-wen-dang2/hao-you-cao-zuo/setFriendPermission.md
       */
      setFriendPemission: async (params, options) => {
        const parsed = validators["hao_you_cao_zuo_setFriendPermission"].safeParse(params ?? {});
        if (!parsed.success) {
          const rid = options?.requestId ?? `validation_${Date.now()}`;
          const issues = parsed.error.issues
            .slice(0, 5)
            .map((i) => `${i.path.join(".") || "<root>"}: ${i.message}`)
            .join("; ");
          throw new WkteamValidationError(`参数校验失败: ${issues}`, {
            requestId: rid,
            operationId: "hao_you_cao_zuo_setFriendPermission",
          });
        }
        return client.call({
          operationId: "hao_you_cao_zuo_setFriendPermission",
          method: "POST",
          path: "/setFriendPemission",
          params: parsed.data,
          requiresAuth: true,
          retryCategory: "side_effect",
          options,
        });
      },

      /**
       * 设置聊天置顶
       *
       * - method: POST
       * - path: /setTop
       * - doc: docs/api/api-wen-dang2/hao-you-cao-zuo/setTop.md
       */
      setTop: async (params, options) => {
        const parsed = validators["hao_you_cao_zuo_setTop"].safeParse(params ?? {});
        if (!parsed.success) {
          const rid = options?.requestId ?? `validation_${Date.now()}`;
          const issues = parsed.error.issues
            .slice(0, 5)
            .map((i) => `${i.path.join(".") || "<root>"}: ${i.message}`)
            .join("; ");
          throw new WkteamValidationError(`参数校验失败: ${issues}`, {
            requestId: rid,
            operationId: "hao_you_cao_zuo_setTop",
          });
        }
        return client.call({
          operationId: "hao_you_cao_zuo_setTop",
          method: "POST",
          path: "/setTop",
          params: parsed.data,
          requiresAuth: true,
          retryCategory: "side_effect",
          options,
        });
      },

      /**
       * 删除好友
       *
       * - method: POST
       * - path: /delContact
       * - doc: docs/api/api-wen-dang2/hao-you-cao-zuo/shan-chu-hao-you.md
       */
      delContact: async (params, options) => {
        const parsed = validators["hao_you_cao_zuo_shan_chu_hao_you"].safeParse(params ?? {});
        if (!parsed.success) {
          const rid = options?.requestId ?? `validation_${Date.now()}`;
          const issues = parsed.error.issues
            .slice(0, 5)
            .map((i) => `${i.path.join(".") || "<root>"}: ${i.message}`)
            .join("; ");
          throw new WkteamValidationError(`参数校验失败: ${issues}`, {
            requestId: rid,
            operationId: "hao_you_cao_zuo_shan_chu_hao_you",
          });
        }
        return client.call({
          operationId: "hao_you_cao_zuo_shan_chu_hao_you",
          method: "POST",
          path: "/delContact",
          params: parsed.data,
          requiresAuth: true,
          retryCategory: "side_effect",
          options,
        });
      },

      /**
       * 设置个人头头像
       *
       * - method: POST
       * - path: /sendHeadImage
       * - doc: docs/api/api-wen-dang2/hao-you-cao-zuo/she-zhi-ge-ren-tou-tou-xiang.md
       */
      sendHeadImage: async (params, options) => {
        const parsed = validators["hao_you_cao_zuo_she_zhi_ge_ren_tou_tou_xiang"].safeParse(params ?? {});
        if (!parsed.success) {
          const rid = options?.requestId ?? `validation_${Date.now()}`;
          const issues = parsed.error.issues
            .slice(0, 5)
            .map((i) => `${i.path.join(".") || "<root>"}: ${i.message}`)
            .join("; ");
          throw new WkteamValidationError(`参数校验失败: ${issues}`, {
            requestId: rid,
            operationId: "hao_you_cao_zuo_she_zhi_ge_ren_tou_tou_xiang",
          });
        }
        return client.call({
          operationId: "hao_you_cao_zuo_she_zhi_ge_ren_tou_tou_xiang",
          method: "POST",
          path: "/sendHeadImage",
          params: parsed.data,
          requiresAuth: true,
          retryCategory: "side_effect",
          options,
        });
      },

      /**
       * 添加隐私设置
       *
       * - method: POST
       * - path: /userPrivacySettings
       * - doc: docs/api/api-wen-dang2/hao-you-cao-zuo/userPrivacySettings.md
       */
      userPrivacySettings: async (params, options) => {
        const parsed = validators["hao_you_cao_zuo_userPrivacySettings"].safeParse(params ?? {});
        if (!parsed.success) {
          const rid = options?.requestId ?? `validation_${Date.now()}`;
          const issues = parsed.error.issues
            .slice(0, 5)
            .map((i) => `${i.path.join(".") || "<root>"}: ${i.message}`)
            .join("; ");
          throw new WkteamValidationError(`参数校验失败: ${issues}`, {
            requestId: rid,
            operationId: "hao_you_cao_zuo_userPrivacySettings",
          });
        }
        return client.call({
          operationId: "hao_you_cao_zuo_userPrivacySettings",
          method: "POST",
          path: "/userPrivacySettings",
          params: parsed.data,
          requiresAuth: true,
          retryCategory: "side_effect",
          options,
        });
      },

      /**
       * 修改好友备注
       *
       * - method: POST
       * - path: /modifyRemark
       * - doc: docs/api/api-wen-dang2/hao-you-cao-zuo/xiu-gai-hao-you-bei-zhu.md
       */
      modifyRemark: async (params, options) => {
        const parsed = validators["hao_you_cao_zuo_xiu_gai_hao_you_bei_zhu"].safeParse(params ?? {});
        if (!parsed.success) {
          const rid = options?.requestId ?? `validation_${Date.now()}`;
          const issues = parsed.error.issues
            .slice(0, 5)
            .map((i) => `${i.path.join(".") || "<root>"}: ${i.message}`)
            .join("; ");
          throw new WkteamValidationError(`参数校验失败: ${issues}`, {
            requestId: rid,
            operationId: "hao_you_cao_zuo_xiu_gai_hao_you_bei_zhu",
          });
        }
        return client.call({
          operationId: "hao_you_cao_zuo_xiu_gai_hao_you_bei_zhu",
          method: "POST",
          path: "/modifyRemark",
          params: parsed.data,
          requiresAuth: true,
          retryCategory: "side_effect",
          options,
        });
      },
    },
    pengYouQuan: {
      /**
       * 异步发送视频朋友圈
       *
       * - method: POST
       * - path: /asynSnsSendVideo
       * - doc: docs/api/api-wen-dang2/peng-you-quan/asynSnsSendVideo.md
       */
      asynSnsSendVideo: async (params, options) => {
        const parsed = validators["peng_you_quan_asynSnsSendVideo"].safeParse(params ?? {});
        if (!parsed.success) {
          const rid = options?.requestId ?? `validation_${Date.now()}`;
          const issues = parsed.error.issues
            .slice(0, 5)
            .map((i) => `${i.path.join(".") || "<root>"}: ${i.message}`)
            .join("; ");
          throw new WkteamValidationError(`参数校验失败: ${issues}`, {
            requestId: rid,
            operationId: "peng_you_quan_asynSnsSendVideo",
          });
        }
        return client.call({
          operationId: "peng_you_quan_asynSnsSendVideo",
          method: "POST",
          path: "/asynSnsSendVideo",
          params: parsed.data,
          requiresAuth: true,
          retryCategory: "side_effect",
          options,
        });
      },

      /**
       * 删除朋友圈
       *
       * - method: POST
       * - path: /deleteSns
       * - doc: docs/api/api-wen-dang2/peng-you-quan/deleteSns.md
       */
      deleteSns: async (params, options) => {
        const parsed = validators["peng_you_quan_deleteSns"].safeParse(params ?? {});
        if (!parsed.success) {
          const rid = options?.requestId ?? `validation_${Date.now()}`;
          const issues = parsed.error.issues
            .slice(0, 5)
            .map((i) => `${i.path.join(".") || "<root>"}: ${i.message}`)
            .join("; ");
          throw new WkteamValidationError(`参数校验失败: ${issues}`, {
            requestId: rid,
            operationId: "peng_you_quan_deleteSns",
          });
        }
        return client.call({
          operationId: "peng_you_quan_deleteSns",
          method: "POST",
          path: "/deleteSns",
          params: parsed.data,
          requiresAuth: true,
          retryCategory: "side_effect",
          options,
        });
      },

      /**
       * 下载朋友圈视频
       *
       * - method: POST
       * - path: /downloadSnsVideo
       * - doc: docs/api/api-wen-dang2/peng-you-quan/downloadSnsVideo.md
       */
      downloadSnsVideo: async (params, options) => {
        const parsed = validators["peng_you_quan_downloadSnsVideo"].safeParse(params ?? {});
        if (!parsed.success) {
          const rid = options?.requestId ?? `validation_${Date.now()}`;
          const issues = parsed.error.issues
            .slice(0, 5)
            .map((i) => `${i.path.join(".") || "<root>"}: ${i.message}`)
            .join("; ");
          throw new WkteamValidationError(`参数校验失败: ${issues}`, {
            requestId: rid,
            operationId: "peng_you_quan_downloadSnsVideo",
          });
        }
        return client.call({
          operationId: "peng_you_quan_downloadSnsVideo",
          method: "POST",
          path: "/downloadSnsVideo",
          params: parsed.data,
          requiresAuth: true,
          retryCategory: "safe",
          options,
        });
      },

      /**
       * 转发朋友圈
       *
       * - method: POST
       * - path: /forwardSns
       * - doc: docs/api/api-wen-dang2/peng-you-quan/forwardSns.md
       */
      forwardSns: async (params, options) => {
        const parsed = validators["peng_you_quan_forwardSns"].safeParse(params ?? {});
        if (!parsed.success) {
          const rid = options?.requestId ?? `validation_${Date.now()}`;
          const issues = parsed.error.issues
            .slice(0, 5)
            .map((i) => `${i.path.join(".") || "<root>"}: ${i.message}`)
            .join("; ");
          throw new WkteamValidationError(`参数校验失败: ${issues}`, {
            requestId: rid,
            operationId: "peng_you_quan_forwardSns",
          });
        }
        return client.call({
          operationId: "peng_you_quan_forwardSns",
          method: "POST",
          path: "/forwardSns",
          params: parsed.data,
          requiresAuth: true,
          retryCategory: "side_effect",
          options,
        });
      },

      /**
       * 获取发送视频朋友圈结果
       *
       * - method: POST
       * - path: /getAsynSnsSendVideoRes
       * - doc: docs/api/api-wen-dang2/peng-you-quan/getAsynSnsSendVideoRes.md
       */
      getAsynSnsSendVideoRes: async (params, options) => {
        const parsed = validators["peng_you_quan_getAsynSnsSendVideoRes"].safeParse(params ?? {});
        if (!parsed.success) {
          const rid = options?.requestId ?? `validation_${Date.now()}`;
          const issues = parsed.error.issues
            .slice(0, 5)
            .map((i) => `${i.path.join(".") || "<root>"}: ${i.message}`)
            .join("; ");
          throw new WkteamValidationError(`参数校验失败: ${issues}`, {
            requestId: rid,
            operationId: "peng_you_quan_getAsynSnsSendVideoRes",
          });
        }
        return client.call({
          operationId: "peng_you_quan_getAsynSnsSendVideoRes",
          method: "POST",
          path: "/getAsynSnsSendVideoRes",
          params: parsed.data,
          requiresAuth: true,
          retryCategory: "safe",
          options,
        });
      },

      /**
       * 获取朋友圈
       *
       * - method: POST
       * - path: /getCircle
       * - doc: docs/api/api-wen-dang2/peng-you-quan/getCircle.md
       */
      getCircle: async (params, options) => {
        const parsed = validators["peng_you_quan_getCircle"].safeParse(params ?? {});
        if (!parsed.success) {
          const rid = options?.requestId ?? `validation_${Date.now()}`;
          const issues = parsed.error.issues
            .slice(0, 5)
            .map((i) => `${i.path.join(".") || "<root>"}: ${i.message}`)
            .join("; ");
          throw new WkteamValidationError(`参数校验失败: ${issues}`, {
            requestId: rid,
            operationId: "peng_you_quan_getCircle",
          });
        }
        return client.call({
          operationId: "peng_you_quan_getCircle",
          method: "POST",
          path: "/getCircle",
          params: parsed.data,
          requiresAuth: true,
          retryCategory: "safe",
          options,
        });
      },

      /**
       * 获取某个好友的朋友圈
       *
       * - method: POST
       * - path: /getFriendCircle
       * - doc: docs/api/api-wen-dang2/peng-you-quan/getFriendCircle.md
       */
      getFriendCircle: async (params, options) => {
        const parsed = validators["peng_you_quan_getFriendCircle"].safeParse(params ?? {});
        if (!parsed.success) {
          const rid = options?.requestId ?? `validation_${Date.now()}`;
          const issues = parsed.error.issues
            .slice(0, 5)
            .map((i) => `${i.path.join(".") || "<root>"}: ${i.message}`)
            .join("; ");
          throw new WkteamValidationError(`参数校验失败: ${issues}`, {
            requestId: rid,
            operationId: "peng_you_quan_getFriendCircle",
          });
        }
        return client.call({
          operationId: "peng_you_quan_getFriendCircle",
          method: "POST",
          path: "/getFriendCircle",
          params: parsed.data,
          requiresAuth: true,
          retryCategory: "safe",
          options,
        });
      },

      /**
       * 获取某条朋友圈详细内容
       *
       * - method: POST
       * - path: /getSnsObject
       * - doc: docs/api/api-wen-dang2/peng-you-quan/getSnsObject.md
       */
      getSnsObject: async (params, options) => {
        const parsed = validators["peng_you_quan_getSnsObject"].safeParse(params ?? {});
        if (!parsed.success) {
          const rid = options?.requestId ?? `validation_${Date.now()}`;
          const issues = parsed.error.issues
            .slice(0, 5)
            .map((i) => `${i.path.join(".") || "<root>"}: ${i.message}`)
            .join("; ");
          throw new WkteamValidationError(`参数校验失败: ${issues}`, {
            requestId: rid,
            operationId: "peng_you_quan_getSnsObject",
          });
        }
        return client.call({
          operationId: "peng_you_quan_getSnsObject",
          method: "POST",
          path: "/getSnsObject",
          params: parsed.data,
          requiresAuth: true,
          retryCategory: "safe",
          options,
        });
      },

      /**
       * 取消点赞
       *
       * - method: POST
       * - path: /snsCancelPraise
       * - doc: docs/api/api-wen-dang2/peng-you-quan/snsCancelPraise.md
       */
      snsCancelPraise: async (params, options) => {
        const parsed = validators["peng_you_quan_snsCancelPraise"].safeParse(params ?? {});
        if (!parsed.success) {
          const rid = options?.requestId ?? `validation_${Date.now()}`;
          const issues = parsed.error.issues
            .slice(0, 5)
            .map((i) => `${i.path.join(".") || "<root>"}: ${i.message}`)
            .join("; ");
          throw new WkteamValidationError(`参数校验失败: ${issues}`, {
            requestId: rid,
            operationId: "peng_you_quan_snsCancelPraise",
          });
        }
        return client.call({
          operationId: "peng_you_quan_snsCancelPraise",
          method: "POST",
          path: "/snsCancelPraise",
          params: parsed.data,
          requiresAuth: true,
          retryCategory: "side_effect",
          options,
        });
      },

      /**
       * 朋友圈评论
       *
       * - method: POST
       * - path: /snsComment
       * - doc: docs/api/api-wen-dang2/peng-you-quan/snsComment.md
       */
      snsComment: async (params, options) => {
        const parsed = validators["peng_you_quan_snsComment"].safeParse(params ?? {});
        if (!parsed.success) {
          const rid = options?.requestId ?? `validation_${Date.now()}`;
          const issues = parsed.error.issues
            .slice(0, 5)
            .map((i) => `${i.path.join(".") || "<root>"}: ${i.message}`)
            .join("; ");
          throw new WkteamValidationError(`参数校验失败: ${issues}`, {
            requestId: rid,
            operationId: "peng_you_quan_snsComment",
          });
        }
        return client.call({
          operationId: "peng_you_quan_snsComment",
          method: "POST",
          path: "/snsComment",
          params: parsed.data,
          requiresAuth: true,
          retryCategory: "side_effect",
          options,
        });
      },

      /**
       * 删除某条朋友圈的某条评论
       *
       * - method: POST
       * - path: /snsCommentDel
       * - doc: docs/api/api-wen-dang2/peng-you-quan/snsCommentDel.md
       */
      snsCommentDel: async (params, options) => {
        const parsed = validators["peng_you_quan_snsCommentDel"].safeParse(params ?? {});
        if (!parsed.success) {
          const rid = options?.requestId ?? `validation_${Date.now()}`;
          const issues = parsed.error.issues
            .slice(0, 5)
            .map((i) => `${i.path.join(".") || "<root>"}: ${i.message}`)
            .join("; ");
          throw new WkteamValidationError(`参数校验失败: ${issues}`, {
            requestId: rid,
            operationId: "peng_you_quan_snsCommentDel",
          });
        }
        return client.call({
          operationId: "peng_you_quan_snsCommentDel",
          method: "POST",
          path: "/snsCommentDel",
          params: parsed.data,
          requiresAuth: true,
          retryCategory: "side_effect",
          options,
        });
      },

      /**
       * 朋友圈点赞
       *
       * - method: POST
       * - path: /snsPraise
       * - doc: docs/api/api-wen-dang2/peng-you-quan/snsPraise.md
       */
      snsPraise: async (params, options) => {
        const parsed = validators["peng_you_quan_snsPraise"].safeParse(params ?? {});
        if (!parsed.success) {
          const rid = options?.requestId ?? `validation_${Date.now()}`;
          const issues = parsed.error.issues
            .slice(0, 5)
            .map((i) => `${i.path.join(".") || "<root>"}: ${i.message}`)
            .join("; ");
          throw new WkteamValidationError(`参数校验失败: ${issues}`, {
            requestId: rid,
            operationId: "peng_you_quan_snsPraise",
          });
        }
        return client.call({
          operationId: "peng_you_quan_snsPraise",
          method: "POST",
          path: "/snsPraise",
          params: parsed.data,
          requiresAuth: true,
          retryCategory: "side_effect",
          options,
        });
      },

      /**
       * 朋友圈权限设置
       *
       * - method: POST
       * - path: /snsPrivacySettings
       * - doc: docs/api/api-wen-dang2/peng-you-quan/snsPrivacySettings.md
       */
      snsPrivacySettings: async (params, options) => {
        const parsed = validators["peng_you_quan_snsPrivacySettings"].safeParse(params ?? {});
        if (!parsed.success) {
          const rid = options?.requestId ?? `validation_${Date.now()}`;
          const issues = parsed.error.issues
            .slice(0, 5)
            .map((i) => `${i.path.join(".") || "<root>"}: ${i.message}`)
            .join("; ");
          throw new WkteamValidationError(`参数校验失败: ${issues}`, {
            requestId: rid,
            operationId: "peng_you_quan_snsPrivacySettings",
          });
        }
        return client.call({
          operationId: "peng_you_quan_snsPrivacySettings",
          method: "POST",
          path: "/snsPrivacySettings",
          params: parsed.data,
          requiresAuth: true,
          retryCategory: "side_effect",
          options,
        });
      },

      /**
       * 发送文字朋友圈消息
       *
       * - method: POST
       * - path: /snsSend
       * - doc: docs/api/api-wen-dang2/peng-you-quan/snsSend.md
       */
      snsSend: async (params, options) => {
        const parsed = validators["peng_you_quan_snsSend"].safeParse(params ?? {});
        if (!parsed.success) {
          const rid = options?.requestId ?? `validation_${Date.now()}`;
          const issues = parsed.error.issues
            .slice(0, 5)
            .map((i) => `${i.path.join(".") || "<root>"}: ${i.message}`)
            .join("; ");
          throw new WkteamValidationError(`参数校验失败: ${issues}`, {
            requestId: rid,
            operationId: "peng_you_quan_snsSend",
          });
        }
        return client.call({
          operationId: "peng_you_quan_snsSend",
          method: "POST",
          path: "/snsSend",
          params: parsed.data,
          requiresAuth: true,
          retryCategory: "side_effect",
          options,
        });
      },

      /**
       * 发送图片朋友圈消息
       *
       * - method: POST
       * - path: /snsSendImage
       * - doc: docs/api/api-wen-dang2/peng-you-quan/snsSendImage.md
       */
      snsSendImage: async (params, options) => {
        const parsed = validators["peng_you_quan_snsSendImage"].safeParse(params ?? {});
        if (!parsed.success) {
          const rid = options?.requestId ?? `validation_${Date.now()}`;
          const issues = parsed.error.issues
            .slice(0, 5)
            .map((i) => `${i.path.join(".") || "<root>"}: ${i.message}`)
            .join("; ");
          throw new WkteamValidationError(`参数校验失败: ${issues}`, {
            requestId: rid,
            operationId: "peng_you_quan_snsSendImage",
          });
        }
        return client.call({
          operationId: "peng_you_quan_snsSendImage",
          method: "POST",
          path: "/snsSendImage",
          params: parsed.data,
          requiresAuth: true,
          retryCategory: "side_effect",
          options,
        });
      },

      /**
       * 发送链接朋友圈消息
       *
       * - method: POST
       * - path: /snsSendUrl
       * - doc: docs/api/api-wen-dang2/peng-you-quan/snsSendUrl.md
       */
      snsSendUrl: async (params, options) => {
        const parsed = validators["peng_you_quan_snsSendUrl"].safeParse(params ?? {});
        if (!parsed.success) {
          const rid = options?.requestId ?? `validation_${Date.now()}`;
          const issues = parsed.error.issues
            .slice(0, 5)
            .map((i) => `${i.path.join(".") || "<root>"}: ${i.message}`)
            .join("; ");
          throw new WkteamValidationError(`参数校验失败: ${issues}`, {
            requestId: rid,
            operationId: "peng_you_quan_snsSendUrl",
          });
        }
        return client.call({
          operationId: "peng_you_quan_snsSendUrl",
          method: "POST",
          path: "/snsSendUrl",
          params: parsed.data,
          requiresAuth: true,
          retryCategory: "side_effect",
          options,
        });
      },

      /**
       * 设置某条朋友圈为隐私
       *
       * - method: POST
       * - path: /snsSetAsPrivacy
       * - doc: docs/api/api-wen-dang2/peng-you-quan/snsSetAsPrivacy.md
       */
      snsSetAsPrivacy: async (params, options) => {
        const parsed = validators["peng_you_quan_snsSetAsPrivacy"].safeParse(params ?? {});
        if (!parsed.success) {
          const rid = options?.requestId ?? `validation_${Date.now()}`;
          const issues = parsed.error.issues
            .slice(0, 5)
            .map((i) => `${i.path.join(".") || "<root>"}: ${i.message}`)
            .join("; ");
          throw new WkteamValidationError(`参数校验失败: ${issues}`, {
            requestId: rid,
            operationId: "peng_you_quan_snsSetAsPrivacy",
          });
        }
        return client.call({
          operationId: "peng_you_quan_snsSetAsPrivacy",
          method: "POST",
          path: "/snsSetAsPrivacy",
          params: parsed.data,
          requiresAuth: true,
          retryCategory: "side_effect",
          options,
        });
      },

      /**
       * 设置某条朋友圈为公开
       *
       * - method: POST
       * - path: /snsSetPublic
       * - doc: docs/api/api-wen-dang2/peng-you-quan/snsSetPublic.md
       */
      snsSetPublic: async (params, options) => {
        const parsed = validators["peng_you_quan_snsSetPublic"].safeParse(params ?? {});
        if (!parsed.success) {
          const rid = options?.requestId ?? `validation_${Date.now()}`;
          const issues = parsed.error.issues
            .slice(0, 5)
            .map((i) => `${i.path.join(".") || "<root>"}: ${i.message}`)
            .join("; ");
          throw new WkteamValidationError(`参数校验失败: ${issues}`, {
            requestId: rid,
            operationId: "peng_you_quan_snsSetPublic",
          });
        }
        return client.call({
          operationId: "peng_you_quan_snsSetPublic",
          method: "POST",
          path: "/snsSetPublic",
          params: parsed.data,
          requiresAuth: true,
          retryCategory: "side_effect",
          options,
        });
      },
    },
    qunCaoZuo: {
      /**
       * 自动通过群（url）
       *
       * - method: POST
       * - path: /acceptUrl
       * - doc: docs/api/api-wen-dang2/qun-cao-zuo/acceptMemberGroup.md
       */
      acceptUrl: async (params, options) => {
        const parsed = validators["qun_cao_zuo_acceptMemberGroup"].safeParse(params ?? {});
        if (!parsed.success) {
          const rid = options?.requestId ?? `validation_${Date.now()}`;
          const issues = parsed.error.issues
            .slice(0, 5)
            .map((i) => `${i.path.join(".") || "<root>"}: ${i.message}`)
            .join("; ");
          throw new WkteamValidationError(`参数校验失败: ${issues}`, {
            requestId: rid,
            operationId: "qun_cao_zuo_acceptMemberGroup",
          });
        }
        return client.call({
          operationId: "qun_cao_zuo_acceptMemberGroup",
          method: "POST",
          path: "/acceptUrl",
          params: parsed.data,
          requiresAuth: true,
          retryCategory: "side_effect",
          options,
        });
      },

      /**
       * 邀请群成员（开启群验证）
       *
       * - method: POST
       * - path: /addChatRoomMemberVerify
       * - doc: docs/api/api-wen-dang2/qun-cao-zuo/addChatRoomMemberVerify.md
       */
      addChatRoomMemberVerify: async (params, options) => {
        const parsed = validators["qun_cao_zuo_addChatRoomMemberVerify"].safeParse(params ?? {});
        if (!parsed.success) {
          const rid = options?.requestId ?? `validation_${Date.now()}`;
          const issues = parsed.error.issues
            .slice(0, 5)
            .map((i) => `${i.path.join(".") || "<root>"}: ${i.message}`)
            .join("; ");
          throw new WkteamValidationError(`参数校验失败: ${issues}`, {
            requestId: rid,
            operationId: "qun_cao_zuo_addChatRoomMemberVerify",
          });
        }
        return client.call({
          operationId: "qun_cao_zuo_addChatRoomMemberVerify",
          method: "POST",
          path: "/addChatRoomMemberVerify",
          params: parsed.data,
          requiresAuth: true,
          retryCategory: "side_effect",
          options,
        });
      },

      /**
       * 添加群成员
       *
       * - method: POST
       * - path: /addChatRoomMember
       * - doc: docs/api/api-wen-dang2/qun-cao-zuo/addGroupMember.md
       */
      addChatRoomMember: async (params, options) => {
        const parsed = validators["qun_cao_zuo_addGroupMember"].safeParse(params ?? {});
        if (!parsed.success) {
          const rid = options?.requestId ?? `validation_${Date.now()}`;
          const issues = parsed.error.issues
            .slice(0, 5)
            .map((i) => `${i.path.join(".") || "<root>"}: ${i.message}`)
            .join("; ");
          throw new WkteamValidationError(`参数校验失败: ${issues}`, {
            requestId: rid,
            operationId: "qun_cao_zuo_addGroupMember",
          });
        }
        return client.call({
          operationId: "qun_cao_zuo_addGroupMember",
          method: "POST",
          path: "/addChatRoomMember",
          params: parsed.data,
          requiresAuth: true,
          retryCategory: "side_effect",
          options,
        });
      },

      /**
       * 添加群成员为好友
       *
       * - method: POST
       * - path: /addRoomMemberFriend
       * - doc: docs/api/api-wen-dang2/qun-cao-zuo/addRoomMemberFriend.md
       */
      addRoomMemberFriend: async (params, options) => {
        const parsed = validators["qun_cao_zuo_addRoomMemberFriend"].safeParse(params ?? {});
        if (!parsed.success) {
          const rid = options?.requestId ?? `validation_${Date.now()}`;
          const issues = parsed.error.issues
            .slice(0, 5)
            .map((i) => `${i.path.join(".") || "<root>"}: ${i.message}`)
            .join("; ");
          throw new WkteamValidationError(`参数校验失败: ${issues}`, {
            requestId: rid,
            operationId: "qun_cao_zuo_addRoomMemberFriend",
          });
        }
        return client.call({
          operationId: "qun_cao_zuo_addRoomMemberFriend",
          method: "POST",
          path: "/addRoomMemberFriend",
          params: parsed.data,
          requiresAuth: true,
          retryCategory: "side_effect",
          options,
        });
      },

      /**
       * 群管理确认入群邀请
       *
       * - method: POST
       * - path: /agreeAddChatRoomMember
       * - doc: docs/api/api-wen-dang2/qun-cao-zuo/agreeAddChatRoomMember.md
       */
      agreeAddChatRoomMember: async (params, options) => {
        const parsed = validators["qun_cao_zuo_agreeAddChatRoomMember"].safeParse(params ?? {});
        if (!parsed.success) {
          const rid = options?.requestId ?? `validation_${Date.now()}`;
          const issues = parsed.error.issues
            .slice(0, 5)
            .map((i) => `${i.path.join(".") || "<root>"}: ${i.message}`)
            .join("; ");
          throw new WkteamValidationError(`参数校验失败: ${issues}`, {
            requestId: rid,
            operationId: "qun_cao_zuo_agreeAddChatRoomMember",
          });
        }
        return client.call({
          operationId: "qun_cao_zuo_agreeAddChatRoomMember",
          method: "POST",
          path: "/agreeAddChatRoomMember",
          params: parsed.data,
          requiresAuth: true,
          retryCategory: "side_effect",
          options,
        });
      },

      /**
       * 创建微信群
       *
       * - method: POST
       * - path: /createChatroom
       * - doc: docs/api/api-wen-dang2/qun-cao-zuo/chuang-jian-wei-xin-qun.md
       */
      createChatroom: async (params, options) => {
        const parsed = validators["qun_cao_zuo_chuang_jian_wei_xin_qun"].safeParse(params ?? {});
        if (!parsed.success) {
          const rid = options?.requestId ?? `validation_${Date.now()}`;
          const issues = parsed.error.issues
            .slice(0, 5)
            .map((i) => `${i.path.join(".") || "<root>"}: ${i.message}`)
            .join("; ");
          throw new WkteamValidationError(`参数校验失败: ${issues}`, {
            requestId: rid,
            operationId: "qun_cao_zuo_chuang_jian_wei_xin_qun",
          });
        }
        return client.call({
          operationId: "qun_cao_zuo_chuang_jian_wei_xin_qun",
          method: "POST",
          path: "/createChatroom",
          params: parsed.data,
          requiresAuth: true,
          retryCategory: "side_effect",
          options,
        });
      },

      /**
       * 删除群成员
       *
       * - method: POST
       * - path: /deleteChatRoomMember
       * - doc: docs/api/api-wen-dang2/qun-cao-zuo/delGroupMember.md
       */
      deleteChatRoomMember: async (params, options) => {
        const parsed = validators["qun_cao_zuo_delGroupMember"].safeParse(params ?? {});
        if (!parsed.success) {
          const rid = options?.requestId ?? `validation_${Date.now()}`;
          const issues = parsed.error.issues
            .slice(0, 5)
            .map((i) => `${i.path.join(".") || "<root>"}: ${i.message}`)
            .join("; ");
          throw new WkteamValidationError(`参数校验失败: ${issues}`, {
            requestId: rid,
            operationId: "qun_cao_zuo_delGroupMember",
          });
        }
        return client.call({
          operationId: "qun_cao_zuo_delGroupMember",
          method: "POST",
          path: "/deleteChatRoomMember",
          params: parsed.data,
          requiresAuth: true,
          retryCategory: "side_effect",
          options,
        });
      },

      /**
       * 邀请群成员（40人以上）
       *
       * - method: POST
       * - path: /inviteChatRoomMember
       * - doc: docs/api/api-wen-dang2/qun-cao-zuo/inviteGroupMember.md
       */
      inviteChatRoomMember: async (params, options) => {
        const parsed = validators["qun_cao_zuo_inviteGroupMember"].safeParse(params ?? {});
        if (!parsed.success) {
          const rid = options?.requestId ?? `validation_${Date.now()}`;
          const issues = parsed.error.issues
            .slice(0, 5)
            .map((i) => `${i.path.join(".") || "<root>"}: ${i.message}`)
            .join("; ");
          throw new WkteamValidationError(`参数校验失败: ${issues}`, {
            requestId: rid,
            operationId: "qun_cao_zuo_inviteGroupMember",
          });
        }
        return client.call({
          operationId: "qun_cao_zuo_inviteGroupMember",
          method: "POST",
          path: "/inviteChatRoomMember",
          params: parsed.data,
          requiresAuth: true,
          retryCategory: "side_effect",
          options,
        });
      },

      /**
       * 群管理操作
       *
       * - method: POST
       * - path: /operateChatRoom
       * - doc: docs/api/api-wen-dang2/qun-cao-zuo/operateChatRoom.md
       */
      operateChatRoom: async (params, options) => {
        const parsed = validators["qun_cao_zuo_operateChatRoom"].safeParse(params ?? {});
        if (!parsed.success) {
          const rid = options?.requestId ?? `validation_${Date.now()}`;
          const issues = parsed.error.issues
            .slice(0, 5)
            .map((i) => `${i.path.join(".") || "<root>"}: ${i.message}`)
            .join("; ");
          throw new WkteamValidationError(`参数校验失败: ${issues}`, {
            requestId: rid,
            operationId: "qun_cao_zuo_operateChatRoom",
          });
        }
        return client.call({
          operationId: "qun_cao_zuo_operateChatRoom",
          method: "POST",
          path: "/operateChatRoom",
          params: parsed.data,
          requiresAuth: true,
          retryCategory: "side_effect",
          options,
        });
      },

      /**
       * 获取群信息
       *
       * - method: POST
       * - path: /getChatRoomInfo
       * - doc: docs/api/api-wen-dang2/qun-cao-zuo/queryGroupDetail.md
       */
      getChatRoomInfo: async (params, options) => {
        const parsed = validators["qun_cao_zuo_queryGroupDetail"].safeParse(params ?? {});
        if (!parsed.success) {
          const rid = options?.requestId ?? `validation_${Date.now()}`;
          const issues = parsed.error.issues
            .slice(0, 5)
            .map((i) => `${i.path.join(".") || "<root>"}: ${i.message}`)
            .join("; ");
          throw new WkteamValidationError(`参数校验失败: ${issues}`, {
            requestId: rid,
            operationId: "qun_cao_zuo_queryGroupDetail",
          });
        }
        return client.call({
          operationId: "qun_cao_zuo_queryGroupDetail",
          method: "POST",
          path: "/getChatRoomInfo",
          params: parsed.data,
          requiresAuth: true,
          retryCategory: "safe",
          options,
        });
      },

      /**
       * 获取群成员
       *
       * - method: POST
       * - path: /getChatRoomMember
       * - doc: docs/api/api-wen-dang2/qun-cao-zuo/queryGroupList.md
       */
      getChatRoomMember: async (params, options) => {
        const parsed = validators["qun_cao_zuo_queryGroupList"].safeParse(params ?? {});
        if (!parsed.success) {
          const rid = options?.requestId ?? `validation_${Date.now()}`;
          const issues = parsed.error.issues
            .slice(0, 5)
            .map((i) => `${i.path.join(".") || "<root>"}: ${i.message}`)
            .join("; ");
          throw new WkteamValidationError(`参数校验失败: ${issues}`, {
            requestId: rid,
            operationId: "qun_cao_zuo_queryGroupList",
          });
        }
        return client.call({
          operationId: "qun_cao_zuo_queryGroupList",
          method: "POST",
          path: "/getChatRoomMember",
          params: parsed.data,
          requiresAuth: true,
          retryCategory: "safe",
          options,
        });
      },

      /**
       * 获取群成员详情
       *
       * - method: POST
       * - path: /getChatRoomMemberInfo
       * - doc: docs/api/api-wen-dang2/qun-cao-zuo/queryGroupMemberDetail.md
       */
      getChatRoomMemberInfo: async (params, options) => {
        const parsed = validators["qun_cao_zuo_queryGroupMemberDetail"].safeParse(params ?? {});
        if (!parsed.success) {
          const rid = options?.requestId ?? `validation_${Date.now()}`;
          const issues = parsed.error.issues
            .slice(0, 5)
            .map((i) => `${i.path.join(".") || "<root>"}: ${i.message}`)
            .join("; ");
          throw new WkteamValidationError(`参数校验失败: ${issues}`, {
            requestId: rid,
            operationId: "qun_cao_zuo_queryGroupMemberDetail",
          });
        }
        return client.call({
          operationId: "qun_cao_zuo_queryGroupMemberDetail",
          method: "POST",
          path: "/getChatRoomMemberInfo",
          params: parsed.data,
          requiresAuth: true,
          retryCategory: "safe",
          options,
        });
      },

      /**
       * 获取群二维码
       *
       * - method: POST
       * - path: /getGroupQrCode
       * - doc: docs/api/api-wen-dang2/qun-cao-zuo/queryGroupQrCode.md
       */
      getGroupQrCode: async (params, options) => {
        const parsed = validators["qun_cao_zuo_queryGroupQrCode"].safeParse(params ?? {});
        if (!parsed.success) {
          const rid = options?.requestId ?? `validation_${Date.now()}`;
          const issues = parsed.error.issues
            .slice(0, 5)
            .map((i) => `${i.path.join(".") || "<root>"}: ${i.message}`)
            .join("; ");
          throw new WkteamValidationError(`参数校验失败: ${issues}`, {
            requestId: rid,
            operationId: "qun_cao_zuo_queryGroupQrCode",
          });
        }
        return client.call({
          operationId: "qun_cao_zuo_queryGroupQrCode",
          method: "POST",
          path: "/getGroupQrCode",
          params: parsed.data,
          requiresAuth: true,
          retryCategory: "safe",
          options,
        });
      },

      /**
       * 退出群聊
       *
       * - method: POST
       * - path: /quitChatRoom
       * - doc: docs/api/api-wen-dang2/qun-cao-zuo/quitGroup.md
       */
      quitChatRoom: async (params, options) => {
        const parsed = validators["qun_cao_zuo_quitGroup"].safeParse(params ?? {});
        if (!parsed.success) {
          const rid = options?.requestId ?? `validation_${Date.now()}`;
          const issues = parsed.error.issues
            .slice(0, 5)
            .map((i) => `${i.path.join(".") || "<root>"}: ${i.message}`)
            .join("; ");
          throw new WkteamValidationError(`参数校验失败: ${issues}`, {
            requestId: rid,
            operationId: "qun_cao_zuo_quitGroup",
          });
        }
        return client.call({
          operationId: "qun_cao_zuo_quitGroup",
          method: "POST",
          path: "/quitChatRoom",
          params: parsed.data,
          requiresAuth: true,
          retryCategory: "side_effect",
          options,
        });
      },

      /**
       * 设置群待办消息
       *
       * - method: POST
       * - path: /roomAppTodo
       * - doc: docs/api/api-wen-dang2/qun-cao-zuo/roomAppTodo.md
       */
      roomAppTodo: async (params, options) => {
        const parsed = validators["qun_cao_zuo_roomAppTodo"].safeParse(params ?? {});
        if (!parsed.success) {
          const rid = options?.requestId ?? `validation_${Date.now()}`;
          const issues = parsed.error.issues
            .slice(0, 5)
            .map((i) => `${i.path.join(".") || "<root>"}: ${i.message}`)
            .join("; ");
          throw new WkteamValidationError(`参数校验失败: ${issues}`, {
            requestId: rid,
            operationId: "qun_cao_zuo_roomAppTodo",
          });
        }
        return client.call({
          operationId: "qun_cao_zuo_roomAppTodo",
          method: "POST",
          path: "/roomAppTodo",
          params: parsed.data,
          requiresAuth: true,
          retryCategory: "side_effect",
          options,
        });
      },

      /**
       * 设置群待办消息
       *
       * - method: POST
       * - path: /roomTodo
       * - doc: docs/api/api-wen-dang2/qun-cao-zuo/roomTodo.md
       */
      roomTodo: async (params, options) => {
        const parsed = validators["qun_cao_zuo_roomTodo"].safeParse(params ?? {});
        if (!parsed.success) {
          const rid = options?.requestId ?? `validation_${Date.now()}`;
          const issues = parsed.error.issues
            .slice(0, 5)
            .map((i) => `${i.path.join(".") || "<root>"}: ${i.message}`)
            .join("; ");
          throw new WkteamValidationError(`参数校验失败: ${issues}`, {
            requestId: rid,
            operationId: "qun_cao_zuo_roomTodo",
          });
        }
        return client.call({
          operationId: "qun_cao_zuo_roomTodo",
          method: "POST",
          path: "/roomTodo",
          params: parsed.data,
          requiresAuth: true,
          retryCategory: "side_effect",
          options,
        });
      },

      /**
       * 群保存|取消到通讯录
       *
       * - method: POST
       * - path: /showInAddressBook
       * - doc: docs/api/api-wen-dang2/qun-cao-zuo/saveGroup.md
       */
      showInAddressBook: async (params, options) => {
        const parsed = validators["qun_cao_zuo_saveGroup"].safeParse(params ?? {});
        if (!parsed.success) {
          const rid = options?.requestId ?? `validation_${Date.now()}`;
          const issues = parsed.error.issues
            .slice(0, 5)
            .map((i) => `${i.path.join(".") || "<root>"}: ${i.message}`)
            .join("; ");
          throw new WkteamValidationError(`参数校验失败: ${issues}`, {
            requestId: rid,
            operationId: "qun_cao_zuo_saveGroup",
          });
        }
        return client.call({
          operationId: "qun_cao_zuo_saveGroup",
          method: "POST",
          path: "/showInAddressBook",
          params: parsed.data,
          requiresAuth: true,
          retryCategory: "side_effect",
          options,
        });
      },

      /**
       * 扫码入群
       *
       * - method: POST
       * - path: /scanJoinRoom
       * - doc: docs/api/api-wen-dang2/qun-cao-zuo/scanJoinRoom.md
       */
      scanJoinRoom: async (params, options) => {
        const parsed = validators["qun_cao_zuo_scanJoinRoom"].safeParse(params ?? {});
        if (!parsed.success) {
          const rid = options?.requestId ?? `validation_${Date.now()}`;
          const issues = parsed.error.issues
            .slice(0, 5)
            .map((i) => `${i.path.join(".") || "<root>"}: ${i.message}`)
            .join("; ");
          throw new WkteamValidationError(`参数校验失败: ${issues}`, {
            requestId: rid,
            operationId: "qun_cao_zuo_scanJoinRoom",
          });
        }
        return client.call({
          operationId: "qun_cao_zuo_scanJoinRoom",
          method: "POST",
          path: "/scanJoinRoom",
          params: parsed.data,
          requiresAuth: true,
          retryCategory: "side_effect",
          options,
        });
      },

      /**
       * 设置群公告
       *
       * - method: POST
       * - path: /setChatRoomAnnouncement
       * - doc: docs/api/api-wen-dang2/qun-cao-zuo/setGroupAnnounct.md
       */
      setChatRoomAnnouncement: async (params, options) => {
        const parsed = validators["qun_cao_zuo_setGroupAnnounct"].safeParse(params ?? {});
        if (!parsed.success) {
          const rid = options?.requestId ?? `validation_${Date.now()}`;
          const issues = parsed.error.issues
            .slice(0, 5)
            .map((i) => `${i.path.join(".") || "<root>"}: ${i.message}`)
            .join("; ");
          throw new WkteamValidationError(`参数校验失败: ${issues}`, {
            requestId: rid,
            operationId: "qun_cao_zuo_setGroupAnnounct",
          });
        }
        return client.call({
          operationId: "qun_cao_zuo_setGroupAnnounct",
          method: "POST",
          path: "/setChatRoomAnnouncement",
          params: parsed.data,
          requiresAuth: true,
          retryCategory: "side_effect",
          options,
        });
      },

      /**
       * 修改群名称
       *
       * - method: POST
       * - path: /modifyGroupName
       * - doc: docs/api/api-wen-dang2/qun-cao-zuo/updateGroupName.md
       */
      modifyGroupName: async (params, options) => {
        const parsed = validators["qun_cao_zuo_updateGroupName"].safeParse(params ?? {});
        if (!parsed.success) {
          const rid = options?.requestId ?? `validation_${Date.now()}`;
          const issues = parsed.error.issues
            .slice(0, 5)
            .map((i) => `${i.path.join(".") || "<root>"}: ${i.message}`)
            .join("; ");
          throw new WkteamValidationError(`参数校验失败: ${issues}`, {
            requestId: rid,
            operationId: "qun_cao_zuo_updateGroupName",
          });
        }
        return client.call({
          operationId: "qun_cao_zuo_updateGroupName",
          method: "POST",
          path: "/modifyGroupName",
          params: parsed.data,
          requiresAuth: true,
          retryCategory: "side_effect",
          options,
        });
      },

      /**
       * 修改群备注
       *
       * - method: POST
       * - path: /modifyGroupRemark
       * - doc: docs/api/api-wen-dang2/qun-cao-zuo/updateGroupRemark.md
       */
      modifyGroupRemark: async (params, options) => {
        const parsed = validators["qun_cao_zuo_updateGroupRemark"].safeParse(params ?? {});
        if (!parsed.success) {
          const rid = options?.requestId ?? `validation_${Date.now()}`;
          const issues = parsed.error.issues
            .slice(0, 5)
            .map((i) => `${i.path.join(".") || "<root>"}: ${i.message}`)
            .join("; ");
          throw new WkteamValidationError(`参数校验失败: ${issues}`, {
            requestId: rid,
            operationId: "qun_cao_zuo_updateGroupRemark",
          });
        }
        return client.call({
          operationId: "qun_cao_zuo_updateGroupRemark",
          method: "POST",
          path: "/modifyGroupRemark",
          params: parsed.data,
          requiresAuth: true,
          retryCategory: "side_effect",
          options,
        });
      },

      /**
       * 修改我在某群的昵称
       *
       * - method: POST
       * - path: /updateIInChatRoomNickName
       * - doc: docs/api/api-wen-dang2/qun-cao-zuo/updateIInChatRoomNickName.md
       */
      updateIInChatRoomNickName: async (params, options) => {
        const parsed = validators["qun_cao_zuo_updateIInChatRoomNickName"].safeParse(params ?? {});
        if (!parsed.success) {
          const rid = options?.requestId ?? `validation_${Date.now()}`;
          const issues = parsed.error.issues
            .slice(0, 5)
            .map((i) => `${i.path.join(".") || "<root>"}: ${i.message}`)
            .join("; ");
          throw new WkteamValidationError(`参数校验失败: ${issues}`, {
            requestId: rid,
            operationId: "qun_cao_zuo_updateIInChatRoomNickName",
          });
        }
        return client.call({
          operationId: "qun_cao_zuo_updateIInChatRoomNickName",
          method: "POST",
          path: "/updateIInChatRoomNickName",
          params: parsed.data,
          requiresAuth: true,
          retryCategory: "side_effect",
          options,
        });
      },
    },
    shipinhao: {
      /**
       * 创建视频号
       *
       * - method: POST
       * - path: /createFinder
       * - doc: docs/api/api-wen-dang2/shipinhao/createFinder.md
       */
      createFinder: async (params, options) => {
        const parsed = validators["shipinhao_createFinder"].safeParse(params ?? {});
        if (!parsed.success) {
          const rid = options?.requestId ?? `validation_${Date.now()}`;
          const issues = parsed.error.issues
            .slice(0, 5)
            .map((i) => `${i.path.join(".") || "<root>"}: ${i.message}`)
            .join("; ");
          throw new WkteamValidationError(`参数校验失败: ${issues}`, {
            requestId: rid,
            operationId: "shipinhao_createFinder",
          });
        }
        return client.call({
          operationId: "shipinhao_createFinder",
          method: "POST",
          path: "/createFinder",
          params: parsed.data,
          requiresAuth: true,
          retryCategory: "side_effect",
          options,
        });
      },

      /**
       * 浏览
       *
       * - method: POST
       * - path: /finderBrowse
       * - doc: docs/api/api-wen-dang2/shipinhao/finderBrowse.md
       */
      finderBrowse: async (params, options) => {
        const parsed = validators["shipinhao_finderBrowse"].safeParse(params ?? {});
        if (!parsed.success) {
          const rid = options?.requestId ?? `validation_${Date.now()}`;
          const issues = parsed.error.issues
            .slice(0, 5)
            .map((i) => `${i.path.join(".") || "<root>"}: ${i.message}`)
            .join("; ");
          throw new WkteamValidationError(`参数校验失败: ${issues}`, {
            requestId: rid,
            operationId: "shipinhao_finderBrowse",
          });
        }
        return client.call({
          operationId: "shipinhao_finderBrowse",
          method: "POST",
          path: "/finderBrowse",
          params: parsed.data,
          requiresAuth: true,
          retryCategory: "side_effect",
          options,
        });
      },

      /**
       * 评论
       *
       * - method: POST
       * - path: /finderComment
       * - doc: docs/api/api-wen-dang2/shipinhao/finderComment.md
       */
      finderComment: async (params, options) => {
        const parsed = validators["shipinhao_finderComment"].safeParse(params ?? {});
        if (!parsed.success) {
          const rid = options?.requestId ?? `validation_${Date.now()}`;
          const issues = parsed.error.issues
            .slice(0, 5)
            .map((i) => `${i.path.join(".") || "<root>"}: ${i.message}`)
            .join("; ");
          throw new WkteamValidationError(`参数校验失败: ${issues}`, {
            requestId: rid,
            operationId: "shipinhao_finderComment",
          });
        }
        return client.call({
          operationId: "shipinhao_finderComment",
          method: "POST",
          path: "/finderComment",
          params: parsed.data,
          requiresAuth: true,
          retryCategory: "side_effect",
          options,
        });
      },

      /**
       * 获取评论列表
       *
       * - method: POST
       * - path: /finderCommentDetails
       * - doc: docs/api/api-wen-dang2/shipinhao/finderCommentDetails.md
       */
      finderCommentDetails: async (params, options) => {
        const parsed = validators["shipinhao_finderCommentDetails"].safeParse(params ?? {});
        if (!parsed.success) {
          const rid = options?.requestId ?? `validation_${Date.now()}`;
          const issues = parsed.error.issues
            .slice(0, 5)
            .map((i) => `${i.path.join(".") || "<root>"}: ${i.message}`)
            .join("; ");
          throw new WkteamValidationError(`参数校验失败: ${issues}`, {
            requestId: rid,
            operationId: "shipinhao_finderCommentDetails",
          });
        }
        return client.call({
          operationId: "shipinhao_finderCommentDetails",
          method: "POST",
          path: "/finderCommentDetails",
          params: parsed.data,
          requiresAuth: true,
          retryCategory: "safe",
          options,
        });
      },

      /**
       * 关注
       *
       * - method: POST
       * - path: /finderFollow
       * - doc: docs/api/api-wen-dang2/shipinhao/finderFollow.md
       */
      finderFollow: async (params, options) => {
        const parsed = validators["shipinhao_finderFollow"].safeParse(params ?? {});
        if (!parsed.success) {
          const rid = options?.requestId ?? `validation_${Date.now()}`;
          const issues = parsed.error.issues
            .slice(0, 5)
            .map((i) => `${i.path.join(".") || "<root>"}: ${i.message}`)
            .join("; ");
          throw new WkteamValidationError(`参数校验失败: ${issues}`, {
            requestId: rid,
            operationId: "shipinhao_finderFollow",
          });
        }
        return client.call({
          operationId: "shipinhao_finderFollow",
          method: "POST",
          path: "/finderFollow",
          params: parsed.data,
          requiresAuth: true,
          retryCategory: "side_effect",
          options,
        });
      },

      /**
       * 获取个人主页
       *
       * - method: POST
       * - path: /finderHome
       * - doc: docs/api/api-wen-dang2/shipinhao/finderHome.md
       */
      finderHome: async (params, options) => {
        const parsed = validators["shipinhao_finderHome"].safeParse(params ?? {});
        if (!parsed.success) {
          const rid = options?.requestId ?? `validation_${Date.now()}`;
          const issues = parsed.error.issues
            .slice(0, 5)
            .map((i) => `${i.path.join(".") || "<root>"}: ${i.message}`)
            .join("; ");
          throw new WkteamValidationError(`参数校验失败: ${issues}`, {
            requestId: rid,
            operationId: "shipinhao_finderHome",
          });
        }
        return client.call({
          operationId: "shipinhao_finderHome",
          method: "POST",
          path: "/finderHome",
          params: parsed.data,
          requiresAuth: true,
          retryCategory: "safe",
          options,
        });
      },

      /**
       * 点赞
       *
       * - method: POST
       * - path: /finderIdFav
       * - doc: docs/api/api-wen-dang2/shipinhao/finderIdFav.md
       */
      finderIdFav: async (params, options) => {
        const parsed = validators["shipinhao_finderIdFav"].safeParse(params ?? {});
        if (!parsed.success) {
          const rid = options?.requestId ?? `validation_${Date.now()}`;
          const issues = parsed.error.issues
            .slice(0, 5)
            .map((i) => `${i.path.join(".") || "<root>"}: ${i.message}`)
            .join("; ");
          throw new WkteamValidationError(`参数校验失败: ${issues}`, {
            requestId: rid,
            operationId: "shipinhao_finderIdFav",
          });
        }
        return client.call({
          operationId: "shipinhao_finderIdFav",
          method: "POST",
          path: "/finderIdFav",
          params: parsed.data,
          requiresAuth: true,
          retryCategory: "side_effect",
          options,
        });
      },

      /**
       * 小红心
       *
       * - method: POST
       * - path: /finderIdLike
       * - doc: docs/api/api-wen-dang2/shipinhao/finderIdLike.md
       */
      finderIdLike: async (params, options) => {
        const parsed = validators["shipinhao_finderIdLike"].safeParse(params ?? {});
        if (!parsed.success) {
          const rid = options?.requestId ?? `validation_${Date.now()}`;
          const issues = parsed.error.issues
            .slice(0, 5)
            .map((i) => `${i.path.join(".") || "<root>"}: ${i.message}`)
            .join("; ");
          throw new WkteamValidationError(`参数校验失败: ${issues}`, {
            requestId: rid,
            operationId: "shipinhao_finderIdLike",
          });
        }
        return client.call({
          operationId: "shipinhao_finderIdLike",
          method: "POST",
          path: "/finderIdLike",
          params: parsed.data,
          requiresAuth: true,
          retryCategory: "side_effect",
          options,
        });
      },

      /**
       * 发布视频号
       *
       * - method: POST
       * - path: /finderPublish
       * - doc: docs/api/api-wen-dang2/shipinhao/finderPublish.md
       */
      finderPublish: async (params, options) => {
        const parsed = validators["shipinhao_finderPublish"].safeParse(params ?? {});
        if (!parsed.success) {
          const rid = options?.requestId ?? `validation_${Date.now()}`;
          const issues = parsed.error.issues
            .slice(0, 5)
            .map((i) => `${i.path.join(".") || "<root>"}: ${i.message}`)
            .join("; ");
          throw new WkteamValidationError(`参数校验失败: ${issues}`, {
            requestId: rid,
            operationId: "shipinhao_finderPublish",
          });
        }
        return client.call({
          operationId: "shipinhao_finderPublish",
          method: "POST",
          path: "/finderPublish",
          params: parsed.data,
          requiresAuth: true,
          retryCategory: "side_effect",
          options,
        });
      },

      /**
       * 上传视频号视频
       *
       * - method: POST
       * - path: /finderUpload
       * - doc: docs/api/api-wen-dang2/shipinhao/finderUpload.md
       */
      finderUpload: async (params, options) => {
        const parsed = validators["shipinhao_finderUpload"].safeParse(params ?? {});
        if (!parsed.success) {
          const rid = options?.requestId ?? `validation_${Date.now()}`;
          const issues = parsed.error.issues
            .slice(0, 5)
            .map((i) => `${i.path.join(".") || "<root>"}: ${i.message}`)
            .join("; ");
          throw new WkteamValidationError(`参数校验失败: ${issues}`, {
            requestId: rid,
            operationId: "shipinhao_finderUpload",
          });
        }
        return client.call({
          operationId: "shipinhao_finderUpload",
          method: "POST",
          path: "/finderUpload",
          params: parsed.data,
          requiresAuth: true,
          retryCategory: "side_effect",
          options,
        });
      },

      /**
       * 获取用户主页
       *
       * - method: POST
       * - path: /finderUserHome
       * - doc: docs/api/api-wen-dang2/shipinhao/finderUserHome.md
       */
      finderUserHome: async (params, options) => {
        const parsed = validators["shipinhao_finderUserHome"].safeParse(params ?? {});
        if (!parsed.success) {
          const rid = options?.requestId ?? `validation_${Date.now()}`;
          const issues = parsed.error.issues
            .slice(0, 5)
            .map((i) => `${i.path.join(".") || "<root>"}: ${i.message}`)
            .join("; ");
          throw new WkteamValidationError(`参数校验失败: ${issues}`, {
            requestId: rid,
            operationId: "shipinhao_finderUserHome",
          });
        }
        return client.call({
          operationId: "shipinhao_finderUserHome",
          method: "POST",
          path: "/finderUserHome",
          params: parsed.data,
          requiresAuth: true,
          retryCategory: "safe",
          options,
        });
      },

      /**
       * 获取私信联系人信息
       *
       * - method: POST
       * - path: /finder/getContactDetails
       * - doc: docs/api/api-wen-dang2/shipinhao/getContactDetails.md
       */
      finderGetContactDetails: async (params, options) => {
        const parsed = validators["shipinhao_getContactDetails"].safeParse(params ?? {});
        if (!parsed.success) {
          const rid = options?.requestId ?? `validation_${Date.now()}`;
          const issues = parsed.error.issues
            .slice(0, 5)
            .map((i) => `${i.path.join(".") || "<root>"}: ${i.message}`)
            .join("; ");
          throw new WkteamValidationError(`参数校验失败: ${issues}`, {
            requestId: rid,
            operationId: "shipinhao_getContactDetails",
          });
        }
        return client.call({
          operationId: "shipinhao_getContactDetails",
          method: "POST",
          path: "/finder/getContactDetails",
          params: parsed.data,
          requiresAuth: true,
          retryCategory: "safe",
          options,
        });
      },

      /**
       * 获取关注列表
       *
       * - method: POST
       * - path: /finder/getFollowList
       * - doc: docs/api/api-wen-dang2/shipinhao/getFollowList.md
       */
      finderGetFollowList: async (params, options) => {
        const parsed = validators["shipinhao_getFollowList"].safeParse(params ?? {});
        if (!parsed.success) {
          const rid = options?.requestId ?? `validation_${Date.now()}`;
          const issues = parsed.error.issues
            .slice(0, 5)
            .map((i) => `${i.path.join(".") || "<root>"}: ${i.message}`)
            .join("; ");
          throw new WkteamValidationError(`参数校验失败: ${issues}`, {
            requestId: rid,
            operationId: "shipinhao_getFollowList",
          });
        }
        return client.call({
          operationId: "shipinhao_getFollowList",
          method: "POST",
          path: "/finder/getFollowList",
          params: parsed.data,
          requiresAuth: true,
          retryCategory: "safe",
          options,
        });
      },

      /**
       * 获取赞和收藏的视频列表
       *
       * - method: POST
       * - path: /finder/getLikeAndFavList
       * - doc: docs/api/api-wen-dang2/shipinhao/getLikeAndFavList.md
       */
      finderGetLikeAndFavList: async (params, options) => {
        const parsed = validators["shipinhao_getLikeAndFavList"].safeParse(params ?? {});
        if (!parsed.success) {
          const rid = options?.requestId ?? `validation_${Date.now()}`;
          const issues = parsed.error.issues
            .slice(0, 5)
            .map((i) => `${i.path.join(".") || "<root>"}: ${i.message}`)
            .join("; ");
          throw new WkteamValidationError(`参数校验失败: ${issues}`, {
            requestId: rid,
            operationId: "shipinhao_getLikeAndFavList",
          });
        }
        return client.call({
          operationId: "shipinhao_getLikeAndFavList",
          method: "POST",
          path: "/finder/getLikeAndFavList",
          params: parsed.data,
          requiresAuth: true,
          retryCategory: "safe",
          options,
        });
      },

      /**
       * 消息列表
       *
       * - method: POST
       * - path: /finder/getMentionList
       * - doc: docs/api/api-wen-dang2/shipinhao/getMentionList.md
       */
      finderGetMentionList: async (params, options) => {
        const parsed = validators["shipinhao_getMentionList"].safeParse(params ?? {});
        if (!parsed.success) {
          const rid = options?.requestId ?? `validation_${Date.now()}`;
          const issues = parsed.error.issues
            .slice(0, 5)
            .map((i) => `${i.path.join(".") || "<root>"}: ${i.message}`)
            .join("; ");
          throw new WkteamValidationError(`参数校验失败: ${issues}`, {
            requestId: rid,
            operationId: "shipinhao_getMentionList",
          });
        }
        return client.call({
          operationId: "shipinhao_getMentionList",
          method: "POST",
          path: "/finder/getMentionList",
          params: parsed.data,
          requiresAuth: true,
          retryCategory: "side_effect",
          options,
        });
      },

      /**
       * 获取我的视频号二维码
       *
       * - method: POST
       * - path: /finder/getQrCode
       * - doc: docs/api/api-wen-dang2/shipinhao/getQrCode.md
       */
      finderGetQrCode: async (params, options) => {
        const parsed = validators["shipinhao_getQrCode"].safeParse(params ?? {});
        if (!parsed.success) {
          const rid = options?.requestId ?? `validation_${Date.now()}`;
          const issues = parsed.error.issues
            .slice(0, 5)
            .map((i) => `${i.path.join(".") || "<root>"}: ${i.message}`)
            .join("; ");
          throw new WkteamValidationError(`参数校验失败: ${issues}`, {
            requestId: rid,
            operationId: "shipinhao_getQrCode",
          });
        }
        return client.call({
          operationId: "shipinhao_getQrCode",
          method: "POST",
          path: "/finder/getQrCode",
          params: parsed.data,
          requiresAuth: true,
          retryCategory: "safe",
          options,
        });
      },

      /**
       * 获取私信SessionId
       *
       * - method: POST
       * - path: /getSessionId
       * - doc: docs/api/api-wen-dang2/shipinhao/getSessionId.md
       */
      getSessionId: async (params, options) => {
        const parsed = validators["shipinhao_getSessionId"].safeParse(params ?? {});
        if (!parsed.success) {
          const rid = options?.requestId ?? `validation_${Date.now()}`;
          const issues = parsed.error.issues
            .slice(0, 5)
            .map((i) => `${i.path.join(".") || "<root>"}: ${i.message}`)
            .join("; ");
          throw new WkteamValidationError(`参数校验失败: ${issues}`, {
            requestId: rid,
            operationId: "shipinhao_getSessionId",
          });
        }
        return client.call({
          operationId: "shipinhao_getSessionId",
          method: "POST",
          path: "/getSessionId",
          params: parsed.data,
          requiresAuth: true,
          retryCategory: "safe",
          options,
        });
      },

      /**
       * 修改视频号资料
       *
       * - method: POST
       * - path: /modFinderProfile
       * - doc: docs/api/api-wen-dang2/shipinhao/modFinderProfile.md
       */
      modFinderProfile: async (params, options) => {
        const parsed = validators["shipinhao_modFinderProfile"].safeParse(params ?? {});
        if (!parsed.success) {
          const rid = options?.requestId ?? `validation_${Date.now()}`;
          const issues = parsed.error.issues
            .slice(0, 5)
            .map((i) => `${i.path.join(".") || "<root>"}: ${i.message}`)
            .join("; ");
          throw new WkteamValidationError(`参数校验失败: ${issues}`, {
            requestId: rid,
            operationId: "shipinhao_modFinderProfile",
          });
        }
        return client.call({
          operationId: "shipinhao_modFinderProfile",
          method: "POST",
          path: "/modFinderProfile",
          params: parsed.data,
          requiresAuth: true,
          retryCategory: "side_effect",
          options,
        });
      },

      /**
       * 私信文字
       *
       * - method: POST
       * - path: /privateSend
       * - doc: docs/api/api-wen-dang2/shipinhao/privateSend.md
       */
      privateSend: async (params, options) => {
        const parsed = validators["shipinhao_privateSend"].safeParse(params ?? {});
        if (!parsed.success) {
          const rid = options?.requestId ?? `validation_${Date.now()}`;
          const issues = parsed.error.issues
            .slice(0, 5)
            .map((i) => `${i.path.join(".") || "<root>"}: ${i.message}`)
            .join("; ");
          throw new WkteamValidationError(`参数校验失败: ${issues}`, {
            requestId: rid,
            operationId: "shipinhao_privateSend",
          });
        }
        return client.call({
          operationId: "shipinhao_privateSend",
          method: "POST",
          path: "/privateSend",
          params: parsed.data,
          requiresAuth: true,
          retryCategory: "side_effect",
          options,
        });
      },

      /**
       * 私信图片
       *
       * - method: POST
       * - path: /privateSendImg
       * - doc: docs/api/api-wen-dang2/shipinhao/privateSendImg.md
       */
      privateSendImg: async (params, options) => {
        const parsed = validators["shipinhao_privateSendImg"].safeParse(params ?? {});
        if (!parsed.success) {
          const rid = options?.requestId ?? `validation_${Date.now()}`;
          const issues = parsed.error.issues
            .slice(0, 5)
            .map((i) => `${i.path.join(".") || "<root>"}: ${i.message}`)
            .join("; ");
          throw new WkteamValidationError(`参数校验失败: ${issues}`, {
            requestId: rid,
            operationId: "shipinhao_privateSendImg",
          });
        }
        return client.call({
          operationId: "shipinhao_privateSendImg",
          method: "POST",
          path: "/privateSendImg",
          params: parsed.data,
          requiresAuth: true,
          retryCategory: "side_effect",
          options,
        });
      },

      /**
       * 登录视频号助手
       *
       * - method: POST
       * - path: /scanFinderHelper
       * - doc: docs/api/api-wen-dang2/shipinhao/scanFinderHelper.md
       */
      scanFinderHelper: async (params, options) => {
        const parsed = validators["shipinhao_scanFinderHelper"].safeParse(params ?? {});
        if (!parsed.success) {
          const rid = options?.requestId ?? `validation_${Date.now()}`;
          const issues = parsed.error.issues
            .slice(0, 5)
            .map((i) => `${i.path.join(".") || "<root>"}: ${i.message}`)
            .join("; ");
          throw new WkteamValidationError(`参数校验失败: ${issues}`, {
            requestId: rid,
            operationId: "shipinhao_scanFinderHelper",
          });
        }
        return client.call({
          operationId: "shipinhao_scanFinderHelper",
          method: "POST",
          path: "/scanFinderHelper",
          params: parsed.data,
          requiresAuth: true,
          retryCategory: "side_effect",
          options,
        });
      },

      /**
       * 搜索视频号
       *
       * - method: POST
       * - path: /newSearchFinder
       * - doc: docs/api/api-wen-dang2/shipinhao/searchFinder.md
       */
      newSearchFinder: async (params, options) => {
        const parsed = validators["shipinhao_searchFinder"].safeParse(params ?? {});
        if (!parsed.success) {
          const rid = options?.requestId ?? `validation_${Date.now()}`;
          const issues = parsed.error.issues
            .slice(0, 5)
            .map((i) => `${i.path.join(".") || "<root>"}: ${i.message}`)
            .join("; ");
          throw new WkteamValidationError(`参数校验失败: ${issues}`, {
            requestId: rid,
            operationId: "shipinhao_searchFinder",
          });
        }
        return client.call({
          operationId: "shipinhao_searchFinder",
          method: "POST",
          path: "/newSearchFinder",
          params: parsed.data,
          requiresAuth: true,
          retryCategory: "side_effect",
          options,
        });
      },
    },
    shouCangJia: {
      /**
       * 获取收藏夹列表
       *
       * - method: POST
       * - path: /weChatFavorites/favSync
       * - doc: docs/api/api-wen-dang2/shou-cang-jia/huo-qu-shou-cang-jia-lie-biao.md
       */
      weChatFavoritesFavSync: async (params, options) => {
        const parsed = validators["shou_cang_jia_huo_qu_shou_cang_jia_lie_biao"].safeParse(params ?? {});
        if (!parsed.success) {
          const rid = options?.requestId ?? `validation_${Date.now()}`;
          const issues = parsed.error.issues
            .slice(0, 5)
            .map((i) => `${i.path.join(".") || "<root>"}: ${i.message}`)
            .join("; ");
          throw new WkteamValidationError(`参数校验失败: ${issues}`, {
            requestId: rid,
            operationId: "shou_cang_jia_huo_qu_shou_cang_jia_lie_biao",
          });
        }
        return client.call({
          operationId: "shou_cang_jia_huo_qu_shou_cang_jia_lie_biao",
          method: "POST",
          path: "/weChatFavorites/favSync",
          params: parsed.data,
          requiresAuth: true,
          retryCategory: "safe",
          options,
        });
      },

      /**
       * 获取收藏夹内容
       *
       * - method: POST
       * - path: /weChatFavorites/getFavItem
       * - doc: docs/api/api-wen-dang2/shou-cang-jia/huo-qu-shou-cang-jia-nei-rong.md
       */
      weChatFavoritesGetFavItem: async (params, options) => {
        const parsed = validators["shou_cang_jia_huo_qu_shou_cang_jia_nei_rong"].safeParse(params ?? {});
        if (!parsed.success) {
          const rid = options?.requestId ?? `validation_${Date.now()}`;
          const issues = parsed.error.issues
            .slice(0, 5)
            .map((i) => `${i.path.join(".") || "<root>"}: ${i.message}`)
            .join("; ");
          throw new WkteamValidationError(`参数校验失败: ${issues}`, {
            requestId: rid,
            operationId: "shou_cang_jia_huo_qu_shou_cang_jia_nei_rong",
          });
        }
        return client.call({
          operationId: "shou_cang_jia_huo_qu_shou_cang_jia_nei_rong",
          method: "POST",
          path: "/weChatFavorites/getFavItem",
          params: parsed.data,
          requiresAuth: true,
          retryCategory: "safe",
          options,
        });
      },

      /**
       * 删除收藏夹内容
       *
       * - method: POST
       * - path: /weChatFavorites/delFavItem
       * - doc: docs/api/api-wen-dang2/shou-cang-jia/shan-chu-shou-cang-jia-nei-rong.md
       */
      weChatFavoritesDelFavItem: async (params, options) => {
        const parsed = validators["shou_cang_jia_shan_chu_shou_cang_jia_nei_rong"].safeParse(params ?? {});
        if (!parsed.success) {
          const rid = options?.requestId ?? `validation_${Date.now()}`;
          const issues = parsed.error.issues
            .slice(0, 5)
            .map((i) => `${i.path.join(".") || "<root>"}: ${i.message}`)
            .join("; ");
          throw new WkteamValidationError(`参数校验失败: ${issues}`, {
            requestId: rid,
            operationId: "shou_cang_jia_shan_chu_shou_cang_jia_nei_rong",
          });
        }
        return client.call({
          operationId: "shou_cang_jia_shan_chu_shou_cang_jia_nei_rong",
          method: "POST",
          path: "/weChatFavorites/delFavItem",
          params: parsed.data,
          requiresAuth: true,
          retryCategory: "side_effect",
          options,
        });
      },
    },
    teShu: {
      /**
       * CDN资源下载
       *
       * - method: POST
       * - path: /cdnDownFile
       * - doc: docs/api/api-wen-dang2/te-shu/cdnDownFile.md
       */
      cdnDownFile: async (params, options) => {
        const parsed = validators["te_shu_cdnDownFile"].safeParse(params ?? {});
        if (!parsed.success) {
          const rid = options?.requestId ?? `validation_${Date.now()}`;
          const issues = parsed.error.issues
            .slice(0, 5)
            .map((i) => `${i.path.join(".") || "<root>"}: ${i.message}`)
            .join("; ");
          throw new WkteamValidationError(`参数校验失败: ${issues}`, {
            requestId: rid,
            operationId: "te_shu_cdnDownFile",
          });
        }
        return client.call({
          operationId: "te_shu_cdnDownFile",
          method: "POST",
          path: "/cdnDownFile",
          params: parsed.data,
          requiresAuth: true,
          retryCategory: "safe",
          options,
        });
      },

      /**
       * 查询接口调用次数
       *
       * - method: POST
       * - path: /getReqTimes
       * - doc: docs/api/api-wen-dang2/te-shu/getReqTimes.md
       */
      getReqTimes: async (params, options) => {
        const parsed = validators["te_shu_getReqTimes"].safeParse(params ?? {});
        if (!parsed.success) {
          const rid = options?.requestId ?? `validation_${Date.now()}`;
          const issues = parsed.error.issues
            .slice(0, 5)
            .map((i) => `${i.path.join(".") || "<root>"}: ${i.message}`)
            .join("; ");
          throw new WkteamValidationError(`参数校验失败: ${issues}`, {
            requestId: rid,
            operationId: "te_shu_getReqTimes",
          });
        }
        return client.call({
          operationId: "te_shu_getReqTimes",
          method: "POST",
          path: "/getReqTimes",
          params: parsed.data,
          requiresAuth: true,
          retryCategory: "safe",
          options,
        });
      },

      /**
       * 查询使用流量
       *
       * - method: POST
       * - path: /getUserFlow
       * - doc: docs/api/api-wen-dang2/te-shu/getUserFlow.md
       */
      getUserFlow: async (params, options) => {
        const parsed = validators["te_shu_getUserFlow"].safeParse(params ?? {});
        if (!parsed.success) {
          const rid = options?.requestId ?? `validation_${Date.now()}`;
          const issues = parsed.error.issues
            .slice(0, 5)
            .map((i) => `${i.path.join(".") || "<root>"}: ${i.message}`)
            .join("; ");
          throw new WkteamValidationError(`参数校验失败: ${issues}`, {
            requestId: rid,
            operationId: "te_shu_getUserFlow",
          });
        }
        return client.call({
          operationId: "te_shu_getUserFlow",
          method: "POST",
          path: "/getUserFlow",
          params: parsed.data,
          requiresAuth: true,
          retryCategory: "safe",
          options,
        });
      },

      /**
       * 查询掉线原因
       *
       * - method: POST
       * - path: /offlineReason
       * - doc: docs/api/api-wen-dang2/te-shu/offlineReason.md
       */
      offlineReason: async (params, options) => {
        const parsed = validators["te_shu_offlineReason"].safeParse(params ?? {});
        if (!parsed.success) {
          const rid = options?.requestId ?? `validation_${Date.now()}`;
          const issues = parsed.error.issues
            .slice(0, 5)
            .map((i) => `${i.path.join(".") || "<root>"}: ${i.message}`)
            .join("; ");
          throw new WkteamValidationError(`参数校验失败: ${issues}`, {
            requestId: rid,
            operationId: "te_shu_offlineReason",
          });
        }
        return client.call({
          operationId: "te_shu_offlineReason",
          method: "POST",
          path: "/offlineReason",
          params: parsed.data,
          requiresAuth: true,
          retryCategory: "safe",
          options,
        });
      },

      /**
       * CDN视频上传
       *
       * - method: POST
       * - path: /sendCdnVideo
       * - doc: docs/api/api-wen-dang2/te-shu/sendCdnVideo.md
       */
      sendCdnVideo: async (params, options) => {
        const parsed = validators["te_shu_sendCdnVideo"].safeParse(params ?? {});
        if (!parsed.success) {
          const rid = options?.requestId ?? `validation_${Date.now()}`;
          const issues = parsed.error.issues
            .slice(0, 5)
            .map((i) => `${i.path.join(".") || "<root>"}: ${i.message}`)
            .join("; ");
          throw new WkteamValidationError(`参数校验失败: ${issues}`, {
            requestId: rid,
            operationId: "te_shu_sendCdnVideo",
          });
        }
        return client.call({
          operationId: "te_shu_sendCdnVideo",
          method: "POST",
          path: "/sendCdnVideo",
          params: parsed.data,
          requiresAuth: true,
          retryCategory: "side_effect",
          options,
        });
      },

      /**
       * 动态设置代理IP
       *
       * - method: POST
       * - path: /setproxy
       * - doc: docs/api/api-wen-dang2/te-shu/setproxy.md
       */
      setproxy: async (params, options) => {
        const parsed = validators["te_shu_setproxy"].safeParse(params ?? {});
        if (!parsed.success) {
          const rid = options?.requestId ?? `validation_${Date.now()}`;
          const issues = parsed.error.issues
            .slice(0, 5)
            .map((i) => `${i.path.join(".") || "<root>"}: ${i.message}`)
            .join("; ");
          throw new WkteamValidationError(`参数校验失败: ${issues}`, {
            requestId: rid,
            operationId: "te_shu_setproxy",
          });
        }
        return client.call({
          operationId: "te_shu_setproxy",
          method: "POST",
          path: "/setproxy",
          params: parsed.data,
          requiresAuth: true,
          retryCategory: "side_effect",
          options,
        });
      },

      /**
       * CDN图片上传
       *
       * - method: POST
       * - path: /uploadCdnImage
       * - doc: docs/api/api-wen-dang2/te-shu/uploadCdnImage.md
       */
      uploadCdnImage: async (params, options) => {
        const parsed = validators["te_shu_uploadCdnImage"].safeParse(params ?? {});
        if (!parsed.success) {
          const rid = options?.requestId ?? `validation_${Date.now()}`;
          const issues = parsed.error.issues
            .slice(0, 5)
            .map((i) => `${i.path.join(".") || "<root>"}: ${i.message}`)
            .join("; ");
          throw new WkteamValidationError(`参数校验失败: ${issues}`, {
            requestId: rid,
            operationId: "te_shu_uploadCdnImage",
          });
        }
        return client.call({
          operationId: "te_shu_uploadCdnImage",
          method: "POST",
          path: "/uploadCdnImage",
          params: parsed.data,
          requiresAuth: true,
          retryCategory: "side_effect",
          options,
        });
      },
    },
    weiXinGuanLi: {
      /**
       * 查询微信是否在线
       *
       * - method: POST
       * - path: /isOnline
       * - doc: docs/api/api-wen-dang2/wei-xin-guan-li/cha-xun-wei-xin-shi-fou-zai-xian.md
       */
      isOnline: async (params, options) => {
        const parsed = validators["wei_xin_guan_li_cha_xun_wei_xin_shi_fou_zai_xian"].safeParse(params ?? {});
        if (!parsed.success) {
          const rid = options?.requestId ?? `validation_${Date.now()}`;
          const issues = parsed.error.issues
            .slice(0, 5)
            .map((i) => `${i.path.join(".") || "<root>"}: ${i.message}`)
            .join("; ");
          throw new WkteamValidationError(`参数校验失败: ${issues}`, {
            requestId: rid,
            operationId: "wei_xin_guan_li_cha_xun_wei_xin_shi_fou_zai_xian",
          });
        }
        return client.call({
          operationId: "wei_xin_guan_li_cha_xun_wei_xin_shi_fou_zai_xian",
          method: "POST",
          path: "/isOnline",
          params: parsed.data,
          requiresAuth: true,
          retryCategory: "safe",
          options,
        });
      },

      /**
       * 查询账号中在线的微信列表
       *
       * - method: POST
       * - path: /queryLoginWx
       * - doc: docs/api/api-wen-dang2/wei-xin-guan-li/duan-xian-chong-lian.md
       */
      queryLoginWx: async (params, options) => {
        const parsed = validators["wei_xin_guan_li_duan_xian_chong_lian"].safeParse(params ?? {});
        if (!parsed.success) {
          const rid = options?.requestId ?? `validation_${Date.now()}`;
          const issues = parsed.error.issues
            .slice(0, 5)
            .map((i) => `${i.path.join(".") || "<root>"}: ${i.message}`)
            .join("; ");
          throw new WkteamValidationError(`参数校验失败: ${issues}`, {
            requestId: rid,
            operationId: "wei_xin_guan_li_duan_xian_chong_lian",
          });
        }
        return client.call({
          operationId: "wei_xin_guan_li_duan_xian_chong_lian",
          method: "POST",
          path: "/queryLoginWx",
          params: parsed.data,
          requiresAuth: true,
          retryCategory: "safe",
          options,
        });
      },

      /**
       * 批量下线微信号
       *
       * - method: POST
       * - path: /member/offline
       * - doc: docs/api/api-wen-dang2/wei-xin-guan-li/pi-liang-xia-xian-wei-xin-hao.md
       */
      memberOffline: async (params, options) => {
        const parsed = validators["wei_xin_guan_li_pi_liang_xia_xian_wei_xin_hao"].safeParse(params ?? {});
        if (!parsed.success) {
          const rid = options?.requestId ?? `validation_${Date.now()}`;
          const issues = parsed.error.issues
            .slice(0, 5)
            .map((i) => `${i.path.join(".") || "<root>"}: ${i.message}`)
            .join("; ");
          throw new WkteamValidationError(`参数校验失败: ${issues}`, {
            requestId: rid,
            operationId: "wei_xin_guan_li_pi_liang_xia_xian_wei_xin_hao",
          });
        }
        return client.call({
          operationId: "wei_xin_guan_li_pi_liang_xia_xian_wei_xin_hao",
          method: "POST",
          path: "/member/offline",
          params: parsed.data,
          requiresAuth: true,
          retryCategory: "side_effect",
          options,
        });
      },
    },
    xiaoXiFaSong: {
      /**
       * 发送emoji表情
       *
       * - method: POST
       * - path: /sendEmoji
       * - doc: docs/api/api-wen-dang2/xiao-xi-fa-song/fa-song-emoji.md
       */
      sendEmoji: async (params, options) => {
        const parsed = validators["xiao_xi_fa_song_fa_song_emoji"].safeParse(params ?? {});
        if (!parsed.success) {
          const rid = options?.requestId ?? `validation_${Date.now()}`;
          const issues = parsed.error.issues
            .slice(0, 5)
            .map((i) => `${i.path.join(".") || "<root>"}: ${i.message}`)
            .join("; ");
          throw new WkteamValidationError(`参数校验失败: ${issues}`, {
            requestId: rid,
            operationId: "xiao_xi_fa_song_fa_song_emoji",
          });
        }
        return client.call({
          operationId: "xiao_xi_fa_song_fa_song_emoji",
          method: "POST",
          path: "/sendEmoji",
          params: parsed.data,
          requiresAuth: true,
          retryCategory: "side_effect",
          options,
        });
      },

      /**
       * 发送链接
       *
       * - method: POST
       * - path: /sendUrl
       * - doc: docs/api/api-wen-dang2/xiao-xi-fa-song/fa-song-lian-jie-xiao-xi.md
       */
      sendUrl: async (params, options) => {
        const parsed = validators["xiao_xi_fa_song_fa_song_lian_jie_xiao_xi"].safeParse(params ?? {});
        if (!parsed.success) {
          const rid = options?.requestId ?? `validation_${Date.now()}`;
          const issues = parsed.error.issues
            .slice(0, 5)
            .map((i) => `${i.path.join(".") || "<root>"}: ${i.message}`)
            .join("; ");
          throw new WkteamValidationError(`参数校验失败: ${issues}`, {
            requestId: rid,
            operationId: "xiao_xi_fa_song_fa_song_lian_jie_xiao_xi",
          });
        }
        return client.call({
          operationId: "xiao_xi_fa_song_fa_song_lian_jie_xiao_xi",
          method: "POST",
          path: "/sendUrl",
          params: parsed.data,
          requiresAuth: true,
          retryCategory: "side_effect",
          options,
        });
      },

      /**
       * 发送名片消息
       *
       * - method: POST
       * - path: /sendNameCard
       * - doc: docs/api/api-wen-dang2/xiao-xi-fa-song/fa-song-ming-pian-xiao-xi.md
       */
      sendNameCard: async (params, options) => {
        const parsed = validators["xiao_xi_fa_song_fa_song_ming_pian_xiao_xi"].safeParse(params ?? {});
        if (!parsed.success) {
          const rid = options?.requestId ?? `validation_${Date.now()}`;
          const issues = parsed.error.issues
            .slice(0, 5)
            .map((i) => `${i.path.join(".") || "<root>"}: ${i.message}`)
            .join("; ");
          throw new WkteamValidationError(`参数校验失败: ${issues}`, {
            requestId: rid,
            operationId: "xiao_xi_fa_song_fa_song_ming_pian_xiao_xi",
          });
        }
        return client.call({
          operationId: "xiao_xi_fa_song_fa_song_ming_pian_xiao_xi",
          method: "POST",
          path: "/sendNameCard",
          params: parsed.data,
          requiresAuth: true,
          retryCategory: "side_effect",
          options,
        });
      },

      /**
       * 发送视频消息
       *
       * - method: POST
       * - path: /sendVideo
       * - doc: docs/api/api-wen-dang2/xiao-xi-fa-song/fa-song-shi-pin-xiao-xi.md
       */
      sendVideo: async (params, options) => {
        const parsed = validators["xiao_xi_fa_song_fa_song_shi_pin_xiao_xi"].safeParse(params ?? {});
        if (!parsed.success) {
          const rid = options?.requestId ?? `validation_${Date.now()}`;
          const issues = parsed.error.issues
            .slice(0, 5)
            .map((i) => `${i.path.join(".") || "<root>"}: ${i.message}`)
            .join("; ");
          throw new WkteamValidationError(`参数校验失败: ${issues}`, {
            requestId: rid,
            operationId: "xiao_xi_fa_song_fa_song_shi_pin_xiao_xi",
          });
        }
        return client.call({
          operationId: "xiao_xi_fa_song_fa_song_shi_pin_xiao_xi",
          method: "POST",
          path: "/sendVideo",
          params: parsed.data,
          requiresAuth: true,
          retryCategory: "side_effect",
          options,
        });
      },

      /**
       * 发送图片消息
       *
       * - method: POST
       * - path: /sendImage2
       * - doc: docs/api/api-wen-dang2/xiao-xi-fa-song/fa-song-tu-pian-xiao-xi2.md
       */
      sendImage2: async (params, options) => {
        const parsed = validators["xiao_xi_fa_song_fa_song_tu_pian_xiao_xi2"].safeParse(params ?? {});
        if (!parsed.success) {
          const rid = options?.requestId ?? `validation_${Date.now()}`;
          const issues = parsed.error.issues
            .slice(0, 5)
            .map((i) => `${i.path.join(".") || "<root>"}: ${i.message}`)
            .join("; ");
          throw new WkteamValidationError(`参数校验失败: ${issues}`, {
            requestId: rid,
            operationId: "xiao_xi_fa_song_fa_song_tu_pian_xiao_xi2",
          });
        }
        return client.call({
          operationId: "xiao_xi_fa_song_fa_song_tu_pian_xiao_xi2",
          method: "POST",
          path: "/sendImage2",
          params: parsed.data,
          requiresAuth: true,
          retryCategory: "side_effect",
          options,
        });
      },

      /**
       * 发送文本消息
       *
       * - method: POST
       * - path: /sendText
       * - doc: docs/api/api-wen-dang2/xiao-xi-fa-song/fa-song-wen-ben-xiao-xi.md
       */
      xiaoXiFaSongFaSongWenBenXiaoXi: async (params, options) => {
        const parsed = validators["xiao_xi_fa_song_fa_song_wen_ben_xiao_xi"].safeParse(params ?? {});
        if (!parsed.success) {
          const rid = options?.requestId ?? `validation_${Date.now()}`;
          const issues = parsed.error.issues
            .slice(0, 5)
            .map((i) => `${i.path.join(".") || "<root>"}: ${i.message}`)
            .join("; ");
          throw new WkteamValidationError(`参数校验失败: ${issues}`, {
            requestId: rid,
            operationId: "xiao_xi_fa_song_fa_song_wen_ben_xiao_xi",
          });
        }
        return client.call({
          operationId: "xiao_xi_fa_song_fa_song_wen_ben_xiao_xi",
          method: "POST",
          path: "/sendText",
          params: parsed.data,
          requiresAuth: true,
          retryCategory: "side_effect",
          options,
        });
      },

      /**
       * 转发视频消息
       *
       * - method: POST
       * - path: /sendRecvVideo
       * - doc: docs/api/api-wen-dang2/xiao-xi-fa-song/fa-song-yi-jing-shou-dao-de-shi-pin-xiao-xi.md
       */
      sendRecvVideo: async (params, options) => {
        const parsed = validators["xiao_xi_fa_song_fa_song_yi_jing_shou_dao_de_shi_pin_xiao_xi"].safeParse(params ?? {});
        if (!parsed.success) {
          const rid = options?.requestId ?? `validation_${Date.now()}`;
          const issues = parsed.error.issues
            .slice(0, 5)
            .map((i) => `${i.path.join(".") || "<root>"}: ${i.message}`)
            .join("; ");
          throw new WkteamValidationError(`参数校验失败: ${issues}`, {
            requestId: rid,
            operationId: "xiao_xi_fa_song_fa_song_yi_jing_shou_dao_de_shi_pin_xiao_xi",
          });
        }
        return client.call({
          operationId: "xiao_xi_fa_song_fa_song_yi_jing_shou_dao_de_shi_pin_xiao_xi",
          method: "POST",
          path: "/sendRecvVideo",
          params: parsed.data,
          requiresAuth: true,
          retryCategory: "side_effect",
          options,
        });
      },

      /**
       * 转发图片消息
       *
       * - method: POST
       * - path: /sendRecvImage
       * - doc: docs/api/api-wen-dang2/xiao-xi-fa-song/fa-song-yi-jing-shou-dao-de-tu-pian-xiao-xi.md
       */
      sendRecvImage: async (params, options) => {
        const parsed = validators["xiao_xi_fa_song_fa_song_yi_jing_shou_dao_de_tu_pian_xiao_xi"].safeParse(params ?? {});
        if (!parsed.success) {
          const rid = options?.requestId ?? `validation_${Date.now()}`;
          const issues = parsed.error.issues
            .slice(0, 5)
            .map((i) => `${i.path.join(".") || "<root>"}: ${i.message}`)
            .join("; ");
          throw new WkteamValidationError(`参数校验失败: ${issues}`, {
            requestId: rid,
            operationId: "xiao_xi_fa_song_fa_song_yi_jing_shou_dao_de_tu_pian_xiao_xi",
          });
        }
        return client.call({
          operationId: "xiao_xi_fa_song_fa_song_yi_jing_shou_dao_de_tu_pian_xiao_xi",
          method: "POST",
          path: "/sendRecvImage",
          params: parsed.data,
          requiresAuth: true,
          retryCategory: "side_effect",
          options,
        });
      },

      /**
       * 转发文件消息
       *
       * - method: POST
       * - path: /sendRecvFile
       * - doc: docs/api/api-wen-dang2/xiao-xi-fa-song/fa-song-yi-jing-shou-dao-de-wen-jian.md
       */
      sendRecvFile: async (params, options) => {
        const parsed = validators["xiao_xi_fa_song_fa_song_yi_jing_shou_dao_de_wen_jian"].safeParse(params ?? {});
        if (!parsed.success) {
          const rid = options?.requestId ?? `validation_${Date.now()}`;
          const issues = parsed.error.issues
            .slice(0, 5)
            .map((i) => `${i.path.join(".") || "<root>"}: ${i.message}`)
            .join("; ");
          throw new WkteamValidationError(`参数校验失败: ${issues}`, {
            requestId: rid,
            operationId: "xiao_xi_fa_song_fa_song_yi_jing_shou_dao_de_wen_jian",
          });
        }
        return client.call({
          operationId: "xiao_xi_fa_song_fa_song_yi_jing_shou_dao_de_wen_jian",
          method: "POST",
          path: "/sendRecvFile",
          params: parsed.data,
          requiresAuth: true,
          retryCategory: "side_effect",
          options,
        });
      },

      /**
       * 发送语音
       *
       * - method: POST
       * - path: /sendVoice
       * - doc: docs/api/api-wen-dang2/xiao-xi-fa-song/fa-song-yu-yin-xiao-xi-ji-jiang-kai-fang.md
       */
      sendVoice: async (params, options) => {
        const parsed = validators["xiao_xi_fa_song_fa_song_yu_yin_xiao_xi_ji_jiang_kai_fang"].safeParse(params ?? {});
        if (!parsed.success) {
          const rid = options?.requestId ?? `validation_${Date.now()}`;
          const issues = parsed.error.issues
            .slice(0, 5)
            .map((i) => `${i.path.join(".") || "<root>"}: ${i.message}`)
            .join("; ");
          throw new WkteamValidationError(`参数校验失败: ${issues}`, {
            requestId: rid,
            operationId: "xiao_xi_fa_song_fa_song_yu_yin_xiao_xi_ji_jiang_kai_fang",
          });
        }
        return client.call({
          operationId: "xiao_xi_fa_song_fa_song_yu_yin_xiao_xi_ji_jiang_kai_fang",
          method: "POST",
          path: "/sendVoice",
          params: parsed.data,
          requiresAuth: true,
          retryCategory: "side_effect",
          options,
        });
      },

      /**
       * 转发链接消息
       *
       * - method: POST
       * - path: /forwardUrl
       * - doc: docs/api/api-wen-dang2/xiao-xi-fa-song/forwardUrl.md
       */
      forwardUrl: async (params, options) => {
        const parsed = validators["xiao_xi_fa_song_forwardUrl"].safeParse(params ?? {});
        if (!parsed.success) {
          const rid = options?.requestId ?? `validation_${Date.now()}`;
          const issues = parsed.error.issues
            .slice(0, 5)
            .map((i) => `${i.path.join(".") || "<root>"}: ${i.message}`)
            .join("; ");
          throw new WkteamValidationError(`参数校验失败: ${issues}`, {
            requestId: rid,
            operationId: "xiao_xi_fa_song_forwardUrl",
          });
        }
        return client.call({
          operationId: "xiao_xi_fa_song_forwardUrl",
          method: "POST",
          path: "/forwardUrl",
          params: parsed.data,
          requiresAuth: true,
          retryCategory: "side_effect",
          options,
        });
      },

      /**
       * 群聊@
       *
       * - method: POST
       * - path: /sendText
       * - doc: docs/api/api-wen-dang2/xiao-xi-fa-song/qun-liao-at.md
       */
      xiaoXiFaSongQunLiaoAt: async (params, options) => {
        const parsed = validators["xiao_xi_fa_song_qun_liao_at"].safeParse(params ?? {});
        if (!parsed.success) {
          const rid = options?.requestId ?? `validation_${Date.now()}`;
          const issues = parsed.error.issues
            .slice(0, 5)
            .map((i) => `${i.path.join(".") || "<root>"}: ${i.message}`)
            .join("; ");
          throw new WkteamValidationError(`参数校验失败: ${issues}`, {
            requestId: rid,
            operationId: "xiao_xi_fa_song_qun_liao_at",
          });
        }
        return client.call({
          operationId: "xiao_xi_fa_song_qun_liao_at",
          method: "POST",
          path: "/sendText",
          params: parsed.data,
          requiresAuth: true,
          retryCategory: "side_effect",
          options,
        });
      },

      /**
       * 撤回消息
       *
       * - method: POST
       * - path: /revokeMsg
       * - doc: docs/api/api-wen-dang2/xiao-xi-fa-song/revokeMsg.md
       */
      revokeMsg: async (params, options) => {
        const parsed = validators["xiao_xi_fa_song_revokeMsg"].safeParse(params ?? {});
        if (!parsed.success) {
          const rid = options?.requestId ?? `validation_${Date.now()}`;
          const issues = parsed.error.issues
            .slice(0, 5)
            .map((i) => `${i.path.join(".") || "<root>"}: ${i.message}`)
            .join("; ");
          throw new WkteamValidationError(`参数校验失败: ${issues}`, {
            requestId: rid,
            operationId: "xiao_xi_fa_song_revokeMsg",
          });
        }
        return client.call({
          operationId: "xiao_xi_fa_song_revokeMsg",
          method: "POST",
          path: "/revokeMsg",
          params: parsed.data,
          requiresAuth: true,
          retryCategory: "side_effect",
          options,
        });
      },

      /**
       * 发送APP类消息
       *
       * - method: POST
       * - path: /sendApplet
       * - doc: docs/api/api-wen-dang2/xiao-xi-fa-song/sendApp.md
       */
      xiaoXiFaSongSendApp: async (params, options) => {
        const parsed = validators["xiao_xi_fa_song_sendApp"].safeParse(params ?? {});
        if (!parsed.success) {
          const rid = options?.requestId ?? `validation_${Date.now()}`;
          const issues = parsed.error.issues
            .slice(0, 5)
            .map((i) => `${i.path.join(".") || "<root>"}: ${i.message}`)
            .join("; ");
          throw new WkteamValidationError(`参数校验失败: ${issues}`, {
            requestId: rid,
            operationId: "xiao_xi_fa_song_sendApp",
          });
        }
        return client.call({
          operationId: "xiao_xi_fa_song_sendApp",
          method: "POST",
          path: "/sendApplet",
          params: parsed.data,
          requiresAuth: true,
          retryCategory: "side_effect",
          options,
        });
      },

      /**
       * 转发小程序
       *
       * - method: POST
       * - path: /sendApplet
       * - doc: docs/api/api-wen-dang2/xiao-xi-fa-song/sendApplet.md
       */
      xiaoXiFaSongSendApplet: async (params, options) => {
        const parsed = validators["xiao_xi_fa_song_sendApplet"].safeParse(params ?? {});
        if (!parsed.success) {
          const rid = options?.requestId ?? `validation_${Date.now()}`;
          const issues = parsed.error.issues
            .slice(0, 5)
            .map((i) => `${i.path.join(".") || "<root>"}: ${i.message}`)
            .join("; ");
          throw new WkteamValidationError(`参数校验失败: ${issues}`, {
            requestId: rid,
            operationId: "xiao_xi_fa_song_sendApplet",
          });
        }
        return client.call({
          operationId: "xiao_xi_fa_song_sendApplet",
          method: "POST",
          path: "/sendApplet",
          params: parsed.data,
          requiresAuth: true,
          retryCategory: "side_effect",
          options,
        });
      },

      /**
       * 发送小程序
       *
       * - method: POST
       * - path: /sendApplets
       * - doc: docs/api/api-wen-dang2/xiao-xi-fa-song/sendApplets.md
       */
      sendApplets: async (params, options) => {
        const parsed = validators["xiao_xi_fa_song_sendApplets"].safeParse(params ?? {});
        if (!parsed.success) {
          const rid = options?.requestId ?? `validation_${Date.now()}`;
          const issues = parsed.error.issues
            .slice(0, 5)
            .map((i) => `${i.path.join(".") || "<root>"}: ${i.message}`)
            .join("; ");
          throw new WkteamValidationError(`参数校验失败: ${issues}`, {
            requestId: rid,
            operationId: "xiao_xi_fa_song_sendApplets",
          });
        }
        return client.call({
          operationId: "xiao_xi_fa_song_sendApplets",
          method: "POST",
          path: "/sendApplets",
          params: parsed.data,
          requiresAuth: true,
          retryCategory: "side_effect",
          options,
        });
      },

      /**
       * 发送文件
       *
       * - method: POST
       * - path: /sendFile
       * - doc: docs/api/api-wen-dang2/xiao-xi-fa-song/sendFile.md
       */
      sendFile: async (params, options) => {
        const parsed = validators["xiao_xi_fa_song_sendFile"].safeParse(params ?? {});
        if (!parsed.success) {
          const rid = options?.requestId ?? `validation_${Date.now()}`;
          const issues = parsed.error.issues
            .slice(0, 5)
            .map((i) => `${i.path.join(".") || "<root>"}: ${i.message}`)
            .join("; ");
          throw new WkteamValidationError(`参数校验失败: ${issues}`, {
            requestId: rid,
            operationId: "xiao_xi_fa_song_sendFile",
          });
        }
        return client.call({
          operationId: "xiao_xi_fa_song_sendFile",
          method: "POST",
          path: "/sendFile",
          params: parsed.data,
          requiresAuth: true,
          retryCategory: "side_effect",
          options,
        });
      },

      /**
       * 发送文件
       *
       * - method: POST
       * - path: /sendFileBase64
       * - doc: docs/api/api-wen-dang2/xiao-xi-fa-song/sendFileBase64.md
       */
      sendFileBase64: async (params, options) => {
        const parsed = validators["xiao_xi_fa_song_sendFileBase64"].safeParse(params ?? {});
        if (!parsed.success) {
          const rid = options?.requestId ?? `validation_${Date.now()}`;
          const issues = parsed.error.issues
            .slice(0, 5)
            .map((i) => `${i.path.join(".") || "<root>"}: ${i.message}`)
            .join("; ");
          throw new WkteamValidationError(`参数校验失败: ${issues}`, {
            requestId: rid,
            operationId: "xiao_xi_fa_song_sendFileBase64",
          });
        }
        return client.call({
          operationId: "xiao_xi_fa_song_sendFileBase64",
          method: "POST",
          path: "/sendFileBase64",
          params: parsed.data,
          requiresAuth: true,
          retryCategory: "side_effect",
          options,
        });
      },
    },
    xiaoXiJieShou: {
      /**
       * 异步下载消息中的视频
       *
       * - method: POST
       * - path: /asynGetMsgVideo
       * - doc: docs/api/api-wen-dang2/xiao-xi-jie-shou/xia-zai-xiao-xi-nei-rong/asynGetMsgVideo.md
       */
      asynGetMsgVideo: async (params, options) => {
        const parsed = validators["xiao_xi_jie_shou_asynGetMsgVideo"].safeParse(params ?? {});
        if (!parsed.success) {
          const rid = options?.requestId ?? `validation_${Date.now()}`;
          const issues = parsed.error.issues
            .slice(0, 5)
            .map((i) => `${i.path.join(".") || "<root>"}: ${i.message}`)
            .join("; ");
          throw new WkteamValidationError(`参数校验失败: ${issues}`, {
            requestId: rid,
            operationId: "xiao_xi_jie_shou_asynGetMsgVideo",
          });
        }
        return client.call({
          operationId: "xiao_xi_jie_shou_asynGetMsgVideo",
          method: "POST",
          path: "/asynGetMsgVideo",
          params: parsed.data,
          requiresAuth: true,
          retryCategory: "safe",
          options,
        });
      },

      /**
       * 下载消息中的动图
       *
       * - method: POST
       * - path: /getMsgEmoji
       * - doc: docs/api/api-wen-dang2/xiao-xi-jie-shou/xia-zai-xiao-xi-nei-rong/getMsgEmoji.md
       */
      getMsgEmoji: async (params, options) => {
        const parsed = validators["xiao_xi_jie_shou_getMsgEmoji"].safeParse(params ?? {});
        if (!parsed.success) {
          const rid = options?.requestId ?? `validation_${Date.now()}`;
          const issues = parsed.error.issues
            .slice(0, 5)
            .map((i) => `${i.path.join(".") || "<root>"}: ${i.message}`)
            .join("; ");
          throw new WkteamValidationError(`参数校验失败: ${issues}`, {
            requestId: rid,
            operationId: "xiao_xi_jie_shou_getMsgEmoji",
          });
        }
        return client.call({
          operationId: "xiao_xi_jie_shou_getMsgEmoji",
          method: "POST",
          path: "/getMsgEmoji",
          params: parsed.data,
          requiresAuth: true,
          retryCategory: "safe",
          options,
        });
      },

      /**
       * 获取异步下载视频消息结果
       *
       * - method: POST
       * - path: /getMsgVideoRes
       * - doc: docs/api/api-wen-dang2/xiao-xi-jie-shou/xia-zai-xiao-xi-nei-rong/getMsgVideoRes.md
       */
      getMsgVideoRes: async (params, options) => {
        const parsed = validators["xiao_xi_jie_shou_getMsgVideoRes"].safeParse(params ?? {});
        if (!parsed.success) {
          const rid = options?.requestId ?? `validation_${Date.now()}`;
          const issues = parsed.error.issues
            .slice(0, 5)
            .map((i) => `${i.path.join(".") || "<root>"}: ${i.message}`)
            .join("; ");
          throw new WkteamValidationError(`参数校验失败: ${issues}`, {
            requestId: rid,
            operationId: "xiao_xi_jie_shou_getMsgVideoRes",
          });
        }
        return client.call({
          operationId: "xiao_xi_jie_shou_getMsgVideoRes",
          method: "POST",
          path: "/getMsgVideoRes",
          params: parsed.data,
          requiresAuth: true,
          retryCategory: "safe",
          options,
        });
      },

      /**
       * 取消消息接收
       *
       * - method: POST
       * - path: /cancelHttpCallbackUrl
       * - doc: docs/api/api-wen-dang2/xiao-xi-jie-shou/shou-xiao-xi/qu-xiao-xiao-xi-jie-shou.md
       */
      cancelHttpCallbackUrl: async (params, options) => {
        const parsed = validators["xiao_xi_jie_shou_qu_xiao_xiao_xi_jie_shou"].safeParse(params ?? {});
        if (!parsed.success) {
          const rid = options?.requestId ?? `validation_${Date.now()}`;
          const issues = parsed.error.issues
            .slice(0, 5)
            .map((i) => `${i.path.join(".") || "<root>"}: ${i.message}`)
            .join("; ");
          throw new WkteamValidationError(`参数校验失败: ${issues}`, {
            requestId: rid,
            operationId: "xiao_xi_jie_shou_qu_xiao_xiao_xi_jie_shou",
          });
        }
        return client.call({
          operationId: "xiao_xi_jie_shou_qu_xiao_xiao_xi_jie_shou",
          method: "POST",
          path: "/cancelHttpCallbackUrl",
          params: parsed.data,
          requiresAuth: true,
          retryCategory: "side_effect",
          options,
        });
      },

      /**
       * 设置消息接收地址
       *
       * - method: POST
       * - path: /setHttpCallbackUrl
       * - doc: docs/api/api-wen-dang2/xiao-xi-jie-shou/shou-xiao-xi/she-zhi-http-hui-tiao-di-zhi.md
       */
      setHttpCallbackUrl: async (params, options) => {
        const parsed = validators["xiao_xi_jie_shou_she_zhi_http_hui_tiao_di_zhi"].safeParse(params ?? {});
        if (!parsed.success) {
          const rid = options?.requestId ?? `validation_${Date.now()}`;
          const issues = parsed.error.issues
            .slice(0, 5)
            .map((i) => `${i.path.join(".") || "<root>"}: ${i.message}`)
            .join("; ");
          throw new WkteamValidationError(`参数校验失败: ${issues}`, {
            requestId: rid,
            operationId: "xiao_xi_jie_shou_she_zhi_http_hui_tiao_di_zhi",
          });
        }
        return client.call({
          operationId: "xiao_xi_jie_shou_she_zhi_http_hui_tiao_di_zhi",
          method: "POST",
          path: "/setHttpCallbackUrl",
          params: parsed.data,
          requiresAuth: false,
          retryCategory: "side_effect",
          options,
        });
      },

      /**
       * 下载图片
       *
       * - method: POST
       * - path: /getMsgImg
       * - doc: docs/api/api-wen-dang2/xiao-xi-jie-shou/xia-zai-xiao-xi-nei-rong/xia-zai-tu-pian-ji-jiang-kai-fang.md
       */
      getMsgImg: async (params, options) => {
        const parsed = validators["xiao_xi_jie_shou_xia_zai_tu_pian_ji_jiang_kai_fang"].safeParse(params ?? {});
        if (!parsed.success) {
          const rid = options?.requestId ?? `validation_${Date.now()}`;
          const issues = parsed.error.issues
            .slice(0, 5)
            .map((i) => `${i.path.join(".") || "<root>"}: ${i.message}`)
            .join("; ");
          throw new WkteamValidationError(`参数校验失败: ${issues}`, {
            requestId: rid,
            operationId: "xiao_xi_jie_shou_xia_zai_tu_pian_ji_jiang_kai_fang",
          });
        }
        return client.call({
          operationId: "xiao_xi_jie_shou_xia_zai_tu_pian_ji_jiang_kai_fang",
          method: "POST",
          path: "/getMsgImg",
          params: parsed.data,
          requiresAuth: true,
          retryCategory: "safe",
          options,
        });
      },

      /**
       * 下载文件
       *
       * - method: POST
       * - path: /getMsgFile
       * - doc: docs/api/api-wen-dang2/xiao-xi-jie-shou/xia-zai-xiao-xi-nei-rong/xia-zai-wen-jian-ji-jiang-kai-fang.md
       */
      getMsgFile: async (params, options) => {
        const parsed = validators["xiao_xi_jie_shou_xia_zai_wen_jian_ji_jiang_kai_fang"].safeParse(params ?? {});
        if (!parsed.success) {
          const rid = options?.requestId ?? `validation_${Date.now()}`;
          const issues = parsed.error.issues
            .slice(0, 5)
            .map((i) => `${i.path.join(".") || "<root>"}: ${i.message}`)
            .join("; ");
          throw new WkteamValidationError(`参数校验失败: ${issues}`, {
            requestId: rid,
            operationId: "xiao_xi_jie_shou_xia_zai_wen_jian_ji_jiang_kai_fang",
          });
        }
        return client.call({
          operationId: "xiao_xi_jie_shou_xia_zai_wen_jian_ji_jiang_kai_fang",
          method: "POST",
          path: "/getMsgFile",
          params: parsed.data,
          requiresAuth: true,
          retryCategory: "safe",
          options,
        });
      },

      /**
       * 下载消息中的语音
       *
       * - method: POST
       * - path: /getMsgVoice
       * - doc: docs/api/api-wen-dang2/xiao-xi-jie-shou/xia-zai-xiao-xi-nei-rong/xia-zai-yu-yin-ji-jiang-kai-fang.md
       */
      getMsgVoice: async (params, options) => {
        const parsed = validators["xiao_xi_jie_shou_xia_zai_yu_yin_ji_jiang_kai_fang"].safeParse(params ?? {});
        if (!parsed.success) {
          const rid = options?.requestId ?? `validation_${Date.now()}`;
          const issues = parsed.error.issues
            .slice(0, 5)
            .map((i) => `${i.path.join(".") || "<root>"}: ${i.message}`)
            .join("; ");
          throw new WkteamValidationError(`参数校验失败: ${issues}`, {
            requestId: rid,
            operationId: "xiao_xi_jie_shou_xia_zai_yu_yin_ji_jiang_kai_fang",
          });
        }
        return client.call({
          operationId: "xiao_xi_jie_shou_xia_zai_yu_yin_ji_jiang_kai_fang",
          method: "POST",
          path: "/getMsgVoice",
          params: parsed.data,
          requiresAuth: true,
          retryCategory: "safe",
          options,
        });
      },
    },
  };
}
